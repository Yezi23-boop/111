# 删除 IMU 测试页面

## 目标
删除 `imu_test_view`（IMU 六轴数据屏幕显示），恢复 `custom_init()` 为正常启动流程。

## 改动清单 (4 处)

### 1. 删除文件
- `main/ui/custom/imu_test_view.c`
- `main/ui/custom/imu_test_view.h`

### 2. 清理 `main/ui/custom/custom.c`
- 移除 `#include "imu_test_view.h"` (line 17)
- 清空 `custom_init()` 中的 IMU 视图创建代码 (lines 36-40)，保留函数壳

### 3. 清理 `main/CMakeLists.txt`
- 移除 `${CMAKE_CURRENT_LIST_DIR}/ui/custom/imu_test_view.c` (line 138)

## 验证
- `idf.py build` 编译通过
- 开机不再进入 IMU 测试页面，正常进入表盘
