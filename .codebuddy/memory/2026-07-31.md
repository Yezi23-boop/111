# 2026-07-31

- 提交暂存区: `5628dbcc feat: 新增独立 HTTPS OTA 上位机与分区整理`(10 文件, +708/-15)。内容: tools/ota_host HTTPS OTA 发布 CLI + 单元测试 + README;分区表删 audio 分区、model 扩至 0x1E0000;同步测试断言与 32MiB OTA 板测证据;新增两个 active 规划文档(独立 HTTPS 双槽 OTA 维护计划、在线音乐播放讨论稿)。
- 注意: `docs/context/plans/active/2026-07-30-standalone-https-ota-maintenance-plan.md` 提交时已从工作区删除(仅存在于 git 历史),后续如需继续该计划需 `git restore` 恢复。
- OTA 全量代码审查完成(4 路并行 subagent,第 3 路中断已人工补查)。结论: P0 无; P1 1 项 = `app_main.c:314-325` IMU/Fall 默认关闭策略错误耦合在 `ota_board_test_start()` 返回值上(当前 kMotionServicesEnabledByDefault=false 掩盖, 需解耦)。P2 关键项: ①全仓 8 个 `*_set_maintenance_active` API 无任何调用点(死代码/协调层未接线, I2C 维护门永不置位); ②ota_service 的 QUIESCING 只 notify 无 wait-ACK(Hermes/BLE 均用现成 request/wait/finish_foreground_quiesce); ③STAGED 后再 START_DOWNLOAD 会 abort 丢弃已 staged 镜像; ④下载无总 deadline; ⑤boot check 用 /resources/README.md 做自检偏弱; ⑥CMakeLists 无条件内嵌 ota_test_ca.pem; ⑦ota_maintenance_view 不可达死代码+潜在 UAF; ⑧ota_host.py 无 >0xC00000 拒绝; ⑨VALID/ROLLED_BACK snapshot 不可达、generation 未写入。待用户决策: maintenance 死代码删除还是接线、quiesce 是否补齐同步等待。
- 用户确认"1.删除": 已删除 8 个全仓无调用的 `*_set_maintenance_active`/`is_maintenance_released` 死代码 API 及配套维护态/锁(通过恢复 16 个文件到 HEAD: i2c_manager, axp2101, qmi8658c, pcf85063atl, touch_ft5x06, mp3_player, audio_app, network_service, official_chat_service, wakeup_evidence_service); 驱动恢复直接 I2C 调用。同步修复 P1: `app_main.c` 中 `ota_board_test_start()` 已与 IMU/Fall 启动策略解耦(独立调用, 不再 else 销毁)。source 测试 12 passed。计划文档 completed/2026-07-30-standalone-https-ota-maintenance-plan.md 已追加收尾记录。
- 审查修正 #2 已按方案 A 落地(用户确认): `ota_service.c` 的 QUIESCING 阶段补齐 `request_foreground_quiesce(&gen) -> wait_foreground_quiesced(gen, 3000ms) -> finish_foreground_quiesce(gen)` 三件套(新增 kQuiesceWaitTimeoutMs=3000U 常量与 quiesce_generation 字段); cleanup_maintenance() 末尾兜底 finish 未结束代次并清零, 满足"abort -> finish_quiesce -> release"反向清理合同。测试断言已固化, source 6 passed, build 通过 (111.bin=0xAB5020)。至此本轮 OTA 审查的所有用户确认项全部完成。
- Runtime Coordinator 方案评审(教学/决策对话): 用户提出"Runtime 可注册资源协调器计划"详细设计, 我完成第一轮评审(6 缺口)后用户补全为可评审详细设计(运行模型=4096B internal task+静态queue+8容量注册表; 双generation=request/transition; 状态机 IDLE/QUIESCING/GRANTING/ACTIVE/ROLLING_BACK/DEGRADED; OTA 不可中断阶段知识归 OTA adapter; background_service_manager 迁移表+改名 Safety Monitor policy owner)。结论: 方向符合嵌入式规范(静态/无堆/事件驱动/回调轻量/不碰资源), 但属"重构换可扩展性"非修故障, 当前 4 份协议能跑无故障, 判定为**非必须、建议存档备胎**, 触发信号=新增第5个前台服务或出现竞态事故。用户尚未决定是否实施。
- 服务生命周期现状调查(教学对话): 全仓服务调查确认当前项目是"常驻模型"——绝大多数服务只有 init/start 无 deinit(coordinator/safety_policy/memory_watch/ota/network/power/weather 等), 仅 official_chat(leave_foreground 别名)/imu/fall_detection 有销毁路径。imu 与 fall_detection 的 destroy 采用"任务内检查标志优雅退出"(非 vTaskDelete 强杀), 且 app_main 默认关闭时复用同一 destroy 路径("默认关闭与动态销毁共用清理路径"是亮点)。会话级可销毁: ws close/ota abort_staging/network provisioning worker 自删。评估结论: 合理, 符合嵌入式"能力常驻、会话可销毁"分层; 唯一建议=official_chat 的 request_shutdown 仅离开前台不释放资源, 需在注释中强调防止误用。教学要点: 嵌入式被创建销毁的永远是"一次性的东西"(会话/临时任务/数据实例), "能力本身"(服务/驱动/同步设施)常驻。
- Safety 关闭释放资源证据链(教学对话, 用户追问"关闭时模型/麦克风是否真释放"): 完整链路 = UI 开关 → policy set_enabled(false) → session apply(false) → danger_detection_service_stop() → espdl_audio_runtime_stop() → cleanup_stopped_runtime_resources()(espdl_audio_runtime.cpp:99-115)。终点站真正释放: ①audio_codec_deinit()(麦克风/codec) ②espdl_model_runner_destroy()(模型内存 free)。即"UI 关闭 = 销毁级释放, 非暂停"。故意不销毁的两处: ①danger_sample_recorder 的 worker/queue 只 reset_session 不销毁(防频繁开关内存碎片, danger_detection_service.c:742 有注释) ②policy/session 静态控制器常驻。教学结论: 关闭时重资源已真释放, policy 自身只有决策无资源故无需 destroy。
- recorder deinit 接线完成(用户确认"增加", build 通过, 无 lint): 危险样本录制器原本是全仓死代码的 deinit(释放 ring buffer 156KB PSRAM + write task 8KB + queue/mutex), 已接到 danger_detection_service_stop() 两个分支(从未运行 & 正常停止), 并新增 danger_sample_recorder_is_initialized() 查询 + start_espdl_backend() 中"未初始化则 init 重建", 保证二次 start 可用。行为变化(已向用户说明): 让路(OTA/前台音频/电源预算)与 UI 关闭现在都走彻底释放, 与模型/codec 既有行为对齐(它们让路时本来就 cleanup_stopped_runtime_resources)。备选方案(让路=暂停、仅关闭=释放)需三层(模型/codec/recorder)一起改造, 未实施, 用户尚未决策。
- PSRAM 碎片风险评估(教学对话): 用户问"反复 deinit/init 156KB 碎片风险大吗"。证据: ①recorder ring buffer 每次申请完全相同大小(5000ms×16000Hz×2B=156.25KB, danger_sample_recorder.c:122), multi_heap 对同大小块反复借还优先复用 free list, 低碎片; ②8MB PSRAM 中占比~1.9%; ③真正风险源=短命大小不一的中块(memory_watch_recorder capture 的 hw_pcm/opus_pcm, recorder 内部 temp_buffer), 与 156KB 块开关不同步; ④项目自认空白: 2026-05-25 计划"补真实板端长期运行堆碎片证据"未勾选, 2026-06-29 run 也写 PSRAM 长期碎片未确认。结论: 理论低风险但无实测数据。已提议在 recorder init/deinit 各加一行 heap_caps_get_largest_free_block(MALLOC_CAP_SPIRAM) 日志做长期观测(改动极小, 未实施, 等用户确认)。
- **用户决策(2026-07-31)**: 碎片观测日志**不加**, 维持现状(recorder 在关闭/让路时统一彻底释放 deinit, 不加固化观测)。recorder deinit 接线改动保持已 build 通过状态, 待烧录验证。备选方案(让路=暂停)也一并搁置。
- 全量资源销毁盘点(教学对话, 用户问"还有什么没彻底销毁"): danger 关闭后已销毁 = 模型内存(model_runner_destroy→delete model+runner)、麦克风/codec(audio_codec_deinit)、推理任务及其栈上 6 个 vector 缓冲(任务退出自动析构, 含 PSRAM fbank)、recorder(156KB ring+8KB task+queue+mutex)。故意不销毁三类: ①纯控制结构(s_runtime/s_service_state/policy/session, 无资源常驻合理) ②模型权重 rodata 在 flash 不占 RAM ③**codec 引用计数保护**: audio_codec_deinit 只是减引用, 引用归零+无会话占用才真关硬件, 故"关闭 danger ≠ 麦克风一定关"(取决于 mp3/AI 对话等其他模块是否占用)。结论: 无遗漏, 三类均为"不该销毁"。
- 全仓服务资源销毁盘点(subagent 70 tool uses): 结论=无真泄漏。分类: **A 常驻能力**(memory_watch 6任务6队列PSRAM store/network/ota/power 4任务/coordinator/weather/time/alerts临时task自删/mini_games/gate)均按"能力随生命周期结束"设计; **B 有销毁且被调用仅2个**: imu(imu_service_destroy→PSRAM ring/window+任务自删, app_main.c:322) 和 fall_detection(fall_detection_service_destroy→模型+3 heap_caps_free+任务自删, app_main.c:321), 与 danger 三胞胎共用默认关闭清理路径。教学总结: 嵌入式生命周期三模式=能力常驻(A)/按需自删(B)/未接线或设计搁置(C)。
- **官方纠正(2026-07-31, 深挖 audio_app+official_chat 后)**: 之前把 official_chat 归为 C 类是错的。证据: official_chat_service_leave_foreground()(official_chat_service.c:1075-1085) 名字轻但投递的是 LEAVE_FOREGROUND_AND_STOP 完整停机命令, 服务任务走 begin_shutdown_from_task→停听→quiet period 等 idle→official_chat_destroy→coordinator 回报, 销毁全程执行。同步版 shutdown()(100ms+轮询)不被 UI 调是因为阻塞 UI, 测试 test_ui_exit_is_frontend_leave_not_blocking_shutdown(189-193) 明确断言 UI 只调 leave_foreground。**official_chat 实为 B 类, 是全仓最完整前台生命周期示范**。真正的 C 类只剩 audio_app 一个: audio_app_start_record/stop_record 全工程零调用(仅 .h/.c 声明实现), record_task 完整但无人创建, audio_app_init 是空壳(仅日志, hardware_init.c:107 调用), 输出路径 /sdcard/record/<timestamp>.wav(docs/storage-and-provisioning-paths.md:18)。推测为早期通用录音 demo, 已被 danger_sample_recorder 和 memory_watch_recorder 取代。待用户决定: 删除或接线。
- **official_chat 无需修复(用户问\"需要修复吗\", 结论=不需要)**: 闭环证据: ①重建路径存在——主循环 930-946 行 s_chat_handle==NULL 且前台 intent 有效时重新走 official_chat_create(739 行), 销毁后 s_chat_handle=NULL 即干净重建, 非\"只销毁不重建\"; ②销毁链完整(prepare_shutdown→停听→quiet period→destroy→清缓存→coordinator 回报); ③UI 调用成对(ai_ui_controller.c:135 enter_foreground / leave_foreground); ④错误路径有兜底(create/set_event_callback/start 任一失败均 destroy+NULL, 744-774 行); ⑤s_chat_handle 注释明确\"仅服务任务负责创建和销毁\"单一 owner。定位: 标准的\"会话级生命周期\"示范(进入创建/离开销毁/可无限循环), 频繁进出仅有 create/destroy 开销属会话语义非缺陷。判断\"需要修复\"的检查表: 销毁后无法重建/销毁不干净/销毁阻塞UI/进出不成对——四项全无。真该处理的仍是 audio_app 孤儿录音。
- **三录音器调查(用户问\"需不需要统一\", 结论=不需要统一)+ audio_app 历史定位(用户让查 context, 已查)**: 三个\"录音器\"根本不是一类东西, 强行统一反而制造耦合——danger_sample_recorder=被动 tap 回调挂 ESP-DL 推理流上(触发窗口前后各1s切片), 输出 PCM WAV+JSON(训练样本不能压缩); memory_watch_recorder=主动申请 AUDIO_CODEC_OWNER_HERMES 拉流, 输出 Ogg Opus(上传省流量), 同步函数; audio_app=主动拉流输出 PCM WAV, 独立 record_task。四项本质差异(输入源/输出格式/触发方式/生命周期)里两项不同就不该抽象。唯一重复点=audio_app 的 generate_wav_header 与 danger_sample_recorder.c:457-472 的 wav_header_t 两份 WAV 头, 但 audio_app 是死代码+44字节结构体, 不值得提取公共库。**audio_app 真正历史定位(context 证据, 纠正昨天\"早期通用 demo\"的推测)**: 诞生于 2026-05-13 麦克风仲裁框架原型(runs/2026-05-13-attempt-safety-monitor-microphone-arbitration.md), 落地了三个\"模式遗产\"且全部活着: ①AUDIO_CODEC_OWNER_AUDIO_RECORDER owner(audio_codec.h:44)现被 audio_mic_test_service.c:406 使用; ②foreground_audio_active 声明机制被 mic_test 完整继承(audio_mic_test_service.c:404); ③policy 阻塞语义在 safety_monitor_policy.c:107。audio_app 本体 start_record/stop_record/record_task/WAV回填=死代码(零调用), audio_app_init 空壳(hardware_init.c:107 调, 仅日志)。删除成本清单: CMakeLists.txt:88 移除+hardware_init.c:107 移除调用+删 .c/.h+**tests/test_audio_codec_port_source.py:49-55 同步改**(它断言 audio_app.c 源码里有 acquire/release(AUDIO_CODEC_OWNER_AUDIO_RECORDER,500U), 删文件会挂; mic_test 已有同样调用被 test_audio_mic_test_service_source.py:44-54 覆盖)。建议=删(audio_app 已被 mic_test 正式继承并超越, 未来做语音备忘录参考 mic_test 更对), 但 AUDIO_CODEC_OWNER_AUDIO_RECORDER 及 policy 阻塞语义是活代码绝不能动。已向用户给出结论, **待用户拍板是否删除**。
- **临界区 API 规范调研(教学对话, 用户让联网搜"esp-idf 项目一般怎么做")**: 结论=官方文档与官方代码"分工不打架"——①**官方文档**(编程指南 v6.0.2《FreeRTOS (IDF)》)明确推荐应用层用 `taskENTER_CRITICAL(&spinlock)` 系列, ISR 用 `taskENTER_CRITICAL_ISR(&spinlock)`; ②**官方组件实际代码**(本地 v5.5.3 components 全量统计): `portENTER_CRITICAL(&lock)` 153+ 处(被截断 93 处)遍布全部驱动(gpio 31/ledc 25/rmt 23/mcpwm ~40/i2c/i2s/adc…), `taskENTER_CRITICAL` 只出现在 FreeRTOS 内核自身(tasks.c 25/queue.c 17/timers.c 6)与 lwip, `_ISR` 版本 56 处驱动 ISR 混用。③**技术关键点**: SMP 内核里 `taskENTER_CRITICAL()` 是无参宏、`portENTER_CRITICAL(...)` 是变参宏——若本项目未来开启 `CONFIG_FREERTOS_SMP`, 现有全部 `taskENTER_CRITICAL(&lock)` 会编译失败, 而 `portENTER_CRITICAL(&lock)` 跨内核稳定。④最终建议(修正此前"统一 task 前缀"): 本项目 legacy 内核下两种等价, 硬规则只有两条=带锁才多核安全(已全合规)+ISR 必须 `_ISR` 版本(当前无此场景); 工程多数已是 task 前缀可统一为 task(符合文档审美), 但须牢记"切 SMP 内核时 task 前缀全返工"的风险。用户已确认执行统一替换：12 个文件全部 `portENTER_CRITICAL`→`taskENTER_CRITICAL`、`portEXIT_CRITICAL`→`taskEXIT_CRITICAL`(replace_all)。涉及 main/services/official_chat_service.c、main/services/network/network_service.c、main/services/memory_watch/memory_watch_service.c 及 components 的 wifi_control、ble_presence、ble_control、ap_portal_routes、ap_portal_adapter、network_manager、network_credentials、system_time、network_provisioning_adapter。替换前已确认无 `_ISR`/`portTRY` 变体(安全)。注意: 并行 replace 时 official_chat_service.c 的 EXIT 替换漏了一批, 重新替换补齐; 搜索残留时出现过过期快照, 以 read_file 实读为准。最终残留=0, `idf.py build` 通过(exitCode=0, 111.bin 生成)。全工程 29 个文件现统一 task 前缀。无 active plan 涉及此改动。**测试同步(用户问"测试需要改吗")**: 2 个 source 契约测试断言 `port` 前缀会挂, 已同步改: tests/test_wifi_control_source.py:57-58 (`portENTER_CRITICAL`/`portEXIT_CRITICAL`→`task` 前缀) + tests/test_memory_watch_service_source.py:487 (`portENTER_CRITICAL(&s_snapshot_lock)`→`taskENTER_CRITICAL(&s_snapshot_lock)`)。`portMUX_TYPE` 是类型名非 API 前缀不用改; docs/context/runs 历史文档保持原样(记录当时事实)。跑 `uv run python -m pytest tests/test_wifi_control_source.py tests/test_memory_watch_service_source.py` = 30 passed。
- **启动日志 STANDBY 语义澄清(教学对话, 用户连问"为什么待机/够电为什么触发/待机不应该保持连接")**: ①STANDBY 与电量无关, 由 `ui_refresh_policy` 无交互 30s 触发(k_standby_timeout_us=30s, ui_refresh_policy.c:36); 低电量(≤20%)只置 LOW_BATTERY_WARN flag(power_policy.c:281-288), 两条路径正交, soc 满也会进 STANDBY。②**知识文档定义(查证 low-power-framework-architecture.md:78-84 + low-power-management-baseline.md:50-51)**: STANDBY = "Wi-Fi 使用 power save, 暂停非关键同步, **不断 AP**", 网络预算三档 FULL/POWER_SAVE/SYNC_PAUSED; "保持连接"指链路层(AP 不断), 业务层云同步/连接属"非关键同步"应暂停。③**memory_watch WebSocket 预连接 bug 定性升级**: 它把"业务层连接"当"链路"跑, 未按 SYNC_PAUSED 语义 gate(health check 有 `network_service_is_service_ready()` gate, 它没有); STANDBY 下网络降级 WIFI_READY + Wi-Fi PS(modem sleep 拉慢 TLS 往返)导致 select() 超时。修复建议不变: `start_foreground_ws()` 补网络就绪门控。用户尚未拍板是否修复。
- **WebSocket 预连接门控修复已完成(2026-07-31)**: 用户确认按方案 B 落地——`memory_watch_service.c` 的 `start_foreground_ws()`(3082 行区域) 在现有门控后新增 `if (!network_service_is_service_ready()) { ESP_LOGD(TAG, "foreground ws preconnect skipped: network not ready"); return; }`, STANDBY 下跳过预连接、回 ACTIVE 后主循环下一轮自动补连。验证: `test_memory_watch_ws_client_source.py` 8 passed + `idf.py build` 通过(111.bin 生成, exitCode=0)。板测未做(可选)。**附带调查结论**: 用户反馈"没进 Hermes 页面就触发", 但代码证据显示 Hermes 预连接只能由 `set_foreground(true)` 触发(仅 `memory_watch_controller_open` 主动进入 / `open_via_notification` 通知自动打开两个入口), 完全没进页面时 `is_foreground_ready()` 必为 false 不可能触发; 另有一套易混淆日志 `official_chat audio channel preconnect`(ai_ui_controller.c:151, AI 实验页)。用户未答澄清直接拍板按原方案修复。
- **audio_app 已删除(2026-07-31, 用户拍板"删除", 全部完成)**: 删除 `main/features/audio/audio_app.c`+.h; `main/CMakeLists.txt` 移除注册; `main/app/hardware_init.c` 移除 include + `audio_app_init()` 调用块。测试同步: `tests/main_paths.py` 移除 `AUDIO_DIR`/`AUDIO_APP_SOURCE`/`AUDIO_APP_HEADER` 并新增 `AUDIO_MIC_TEST_SERVICE_SOURCE`/`AUDIO_MIC_TEST_SERVICE_HEADER`; `tests/test_audio_codec_port_source.py` 的 `test_audio_app_uses_audio_codec_wrapper_and_24khz_header` 重写为 `test_audio_mic_test_uses_audio_codec_wrapper_and_24khz_header`(断言 mic_test 的 acquire/release(AUDIO_CODEC_OWNER_AUDIO_RECORDER, kInputAcquireTimeoutMs)/safety_monitor_policy_set_foreground_audio_active true "mic_test" false "mic_test_done"/AUDIO_PLATFORM_HW_SAMPLE_RATE/INPUT_CHANNELS); `tests/test_board_ds2413_motor_source.py` 的 audio_app_init 断言改为 resource_fs_init。context 库同步(当前生效文档): agent-operational-rules.md 优先关注模块换 audio_mic_test_service.c; audio-codec-address-clock-baseline.md owner 换 mic_test; task-stack-measurement-full-summary.md RecTask 行标注"已随 audio_app 删除"; storage-and-provisioning-paths.md 移除 /sdcard/record 路径与 audio_app_init 入口(补 danger_samples 路径); startup-init-and-blocking-chain.md 移除 audio_app_init; display-render-touch-transfer-pipeline.md 移除 audio_app; main-directory-map.md 删 features/audio 条目补 danger_sample_recorder; repo-overview.md 两处引用清理。历史文档(runs/plans/completed/CHANGELOG)保留原文不改。**验证**: `pytest tests` 453 过 1 挂, 唯一失败 `test_danger_detection_service_source.py::test_recorder_stop_path_resets_session_instead_of_deinit` 是**预存在失败**(断言 stop 路径应调 reset_session() 而非 deinit(), 实际 stop 路径 698-699 行仍 deinit(), reset_session 只在 start 路径 614 行; 该文件本次未触碰, 与删除无关); `idf.py build` 成功(111.bin 生成, app 分区剩 11%)。测试残留仅 `test_ota_service_source.py:35` 的 `assertNotIn("audio_app_set_maintenance_active", ...)`——删除后依然成立, 保留正确。**注意: AUDIO_CODEC_OWNER_AUDIO_RECORDER 及 policy 阻塞语义是活代码(mic_test 在用), 绝不能删。**
- **预存在测试失败已修复(用户拍板"修复", 2026-07-31)**: 上一轮遗留的 `test_recorder_stop_path_resets_session_instead_of_deinit` 失败根因=过时的测试契约。证据链: ①git 确认 HEAD(23ecce36) 的 stop 路径是 reset_session 旧设计, 工作区未提交改动已按用户 2026-07-31 决策改为 deinit(见本条记忆"recorder deinit 接线完成")并带 is_initialized 重建闭环; ②run 2026-07-04 记录的是旧设计, 按规则历史文档不改; ③4 张 danger 卡无 recorder 生命周期描述, 无需同步。修复=更新过时测试: `tests/test_danger_detection_service_source.py` 的该测试重写为 `test_recorder_stop_path_deinits_recorder_for_full_release`(断言 stop 路径含 `danger_sample_recorder_deinit();`、不含 `danger_sample_recorder_reset_session();`、含注释"停止即彻底释放"); 同步修正 `danger_sample_recorder.h` 中 reset_session 的过时 Doxygen(原"用于 start/stop 边界, 不销毁 worker/queue"改为"每次 start 时调用保证新会话干净, 销毁由 stop 的 deinit 负责")。验证: `pytest tests/test_danger_detection_service_source.py` 12 passed + 全量 `pytest tests` 454 passed 全绿(上一轮 453 过 1 挂的遗留已清零)。
- **recorder deinit 接线烧录验证完成(2026-07-31, COM7)**: 先 `idf.py build`(111.bin=0xab5460, app 分区剩 11%) 再用 `agent_serial_monitor.ps1 -Port COM7 -Action app-flash-monitor -DurationSeconds 60` 烧录+采集 60s 日志(日志: board_logs/2026-07-31-19-18-09-recorder_deinit_verify.log, panic_log_seen=false)。冷启动验证结果: ①recorder init 正常(danger_recorder: 已初始化 buffer=3000ms/16000Hz/dir=/sdcard/danger_samples, 即 is_initialized+init+reset_session 启动路径工作正常) ②危险识别启用(power_policy danger=1 保持) ③danger_rec_wr 任务运行(stack 5900B/23%) ④全系统正常(Wi-Fi 192.168.86.64 SERVICE_READY/OTA started/进入 STANDBY)。**验证边界**: deinit 的关闭/让路触发路径冷启动不触发, 运行时触发需用户 UI 手动关闭危险识别, 该路径已由 build+source 测试契约保障。日志中 danger_recorder 中文乱码为 monitor 控制台 UTF-8 显示问题, 非 bug。
- **框架精简-空目录清理完成(2026-07-31)**: 用户反馈"框架臃肿", 盘点确认 6 个疑似空目录中 5 个是规划收敛/迁移后遗留空壳(runtime_gate/startup 被 07-31 runtime_coordinator 收敛, features/audio 因 audio_app 删除变空, features/weather 迁到 services/weather, features/detection 从未落地), 1 个有意保留(features/memory_watch 仅 README, 07-14 设计决策, 不删)。已删 5 个空目录(git 不追踪空目录, 删除零风险), 同步修正 docs/context/knowledge/project/repo-overview.md 第 49 行滞后描述(原把 features/audio、features/weather 列为业务特性, 现注明已收敛到 services/ 且目录已删)。`idf.py build` 通过(exitCode=0, 111.bin 生成)。剩余候选(未执行): CMakeLists 手写全路径源列表改 glob、平铺 service(official_chat_service/fall_detection_service)归入子目录、board test 迁出生产树、features/detection 未落地归属, 均待用户决策。
- **safety_monitor_policy 瘦身完成(2026-07-31, 用户拍板)**: ①删 `resolve_block` 的 `started` 死参数与 NOT_READY 死分支(唯一调用点恒传 true) ②删 `blocked_by_foreground_audio` 冗余字段(context+snapshot, 与 block_reason==FOREGROUND_AUDIO 恒等且全仓无消费者) ③删 `last_error` 字段(context+snapshot) —— **注意**: policy 的 last_error 不是零消费者, `danger_detection_controller.c:75`(danger_detection_status_text 返回"监听异常")才是真消费者, 已改读源头 `safety_monitor_session_get_snapshot().last_error` 并补 include services/safety/safety_monitor_session.h(教训: 首轮 grep 结果被截断误判"无消费者", 靠 build 失败才暴露) ④`set_foreground_audio_active` 的 `reason` 参数从 `(void)reason` 丢弃改为补入 ESP_LOGD 日志(用户选"补用到日志"而非删参数, 4 个调用方 mic_test/memory_watch_recording 诊断字符串从此可观测)。测试同步: test_safety_monitor_session_source.py 删 `blocked_by_foreground_audio` header 断言, `resolve_block` 断言保留(函数未删)。验证: source 测试 5 passed + `idf.py build` 通过(111.bin=0xAB5560, app 分区剩 11%)。**教训**: 同一文件多个 replace_in_file 并行执行会竞态丢修改(本轮 C/F 两处丢失), 同文件编辑必须串行。
- **CMakeLists glob 化准备-断言收集完成(2026-07-31)**: 系统收集了 tests/** 中所有针对 `main/CMakeLists.txt` 的源文件路径断言(25 个测试文件), 整理为清单文档(brain artifact: cmake-source-path-assertions-inventory.md)。要点: ①CMakeLists 5 个手写块(app_srcs 9/service_srcs 22/feature_srcs 9/ui_runtime_srcs 18) + 2 个已 glob 块(ui_generated/ui_custom_fonts) + REQUIRES 手写块; ②所有 `assertIn("<路径>")` 在 glob 后字符串匹配仍通过; ③**3 个高险断言**: test_power_integration_source.py 结构化提取 `app_srcs`/`service_srcs` 命名块(需保留变量名+idf_component_register 结构)、test_runtime_coordinator_source.py 的 `assertNotIn("foreground_runtime_gate.c")` 与 test_ui_chinese_fonts_source.py 的 `assertNotIn("ui_runtime_fonts.c")`(需确认旧文件已删否则被 glob 误收); ④glob 落点建议: app→`app/*.c`, services→GLOB_RECURSE, features→GLOB_RECURSE, ui_runtime→**禁止 GLOB_RECURSE**(防误收 ui/generated、ui/custom/fonts), 需先核实 ui/custom 下子目录结构; ⑤REQUIRES 11 个依赖断言与 glob 无关。下一步待用户决策是否执行 glob 化。
- **外部 skill 仓库更新(2026-07-31)**: `C:\Users\ye\.agents\skill-sources\mattpocock-skills` 快进更新 b8be62f→aaf2453(+5 commits)。内容: 新增 `skills/in-progress/teach/` 教学 skill(5 新文件)、更新 `grill-with-docs/CONTEXT-FORMAT.md`、`to-prd/SKILL.md`。注: ①fetch GitHub 时 TLS/连接被重置失败, 但本地已缓存 origin/main 对象, 用 `git merge --ff-only origin/main` 离线完成; ②仓库仍残留 11 个未提交删除(本地工作区删除: setup-matt-pocock-skills/to-issues/triage/caveman 等), 非本次 pull 造成, 保持原样未提交未恢复, 待用户意图。
- **main/CMakeLists.txt glob 化改造完成(2026-07-31)**: 手写源路径列表 → `file(GLOB)`。规则: `app/*.c`、`services` 与 `features` 用 `GLOB_RECURSE`、`ui/*.c + ui/custom/*.c` 非 RECURSE(防误收 ui/generated 与 fonts)。`tests/main_cmake_contract.py` 新增 `assert_main_source_globbed()` helper(路径→glob 模式映射), 24 个旧式 `assertIn("${CMAKE_CURRENT_LIST_DIR}/...")` 断言全部迁移, `test_power_integration_source.py` 删除 `_extract_named_cmake_set_body()`(glob 后无 set 块), `test_memory_watch_ogg_opus_muxer_source.py` 与 `test_runtime_resource_gate_board_test_source.py` 删除 orphan `MAIN_CMAKE` import。验证: pytest 455 passed 全绿, `idf.py build` 2228/2228 编译通过, 111.bin 生成(app 分区 11% 空闲)。已删文件 `foreground_runtime_gate.c`/`ui_runtime_fonts.c` 的 assertNotIn 测试仍通过。教训: read_file 可能显示旧缓存, 必须用 Python 直读磁盘确认真实内容; 并行 replace_in_file 有竞态风险, 同文件修改须串行。
