# 2026-07-03

- 表情表盘 V1A：用户已完成 SD 卡资源拷贝，把 `sdcard/watchface/` staging 中的 5 个 `.rawanim`（idle/thinking/working/message/error）和 `manifest.json` 拷贝到电脑端 SD 卡目录 `E:\watchface`，对应板端 `/sdcard/watchface`。
- 已同步更新活跃计划 `docs/context/plans/active/2026-06-30-watchface-emoji-root-ui-plan.md` 的进度部分。
- 扩展 `components/sd_card/sd_manager`：新增 `sd_manager_read_range(path, offset, buffer, size, bytes_read)`，基于 `fopen`+`fseek`+`fread` 实现按 rawanim 帧表 offset 随机读定长帧；短读返回 `ESP_ERR_INVALID_SIZE`。已 `idf.py build` 通过（app 11.1MB，剩 23%）。
- 修正计划中发现的 4 个问题：①sd_manager 缺随机读 API（已扩展）②`watchface_anim_info_t.delay_ms` 类型与 header 不一致（改 uint32_t）③帧槽并发同步策略缺失（补 V1B 约束）④V1A 验收未要求实测 SD 读取耗时（已补）。
- 进一步修正帧槽提升链路描述：原写"decode→next→current 全由 worker 推进"不准确。正确分工是 worker 负责 `decode→next`（解码完一帧时），LVGL timer 负责 `next→current`（切帧周期到点时，因 lv_image_set_src 必须在 LVGL 线程调用）。四槽设计目的是让 worker 超前解码 2 帧吸收 SD 抖动。
- 明确 LVGL 与缓存帧的渲染方式：zero-copy，`lv_image_dsc_t.data` 直接指向 PSRAM 帧槽，禁止 memcpy 到额外 buffer。约束已写入计划核心原则。LVGL draw 异步分块读取 current 帧期间，worker 不得覆写该帧槽，这是四槽 + current 保护的根本原因。
- 完成 V1A `watchface_anim_cache` 实现：新增 4 个文件 `watchface_anim_assets.h/c` + `watchface_anim_cache.h/c`。rawanim header 实测 28 字节（struct `<8sHHHHHHII>` = magic8+version2+w2+h2+format2+frame_count2+fps2+frame_duration_us4+data_offset4）。帧槽用 `MALLOC_CAP_SPIRAM`，zero-copy 返回指针。read_frame 用 `sd_manager_read_range` 按帧表 offset 随机读，记录耗时基线日志。14 条 source test 全通过，`idf.py build` 通过（app 11.1MB 剩 23%）。
- V1A 已完成：probe/load_state/read_frame/current_frame/deinit。V1B 待做：四槽轮转（current/next/decode/standby）+ 后台 decode worker + LVGL timer 切帧 + watchface_view + watchface_controller + 根首页导航。
- V1A 下一步：实现 `watchface_anim_cache`（SD 读取 + PSRAM 帧槽 + rawanim 包头校验 + 单帧 probe + 读取耗时基线日志），不接 UI/导航。
- 实现 V1B `watchface_view`：创建 `watchface_view.h` 和 `watchface_view.c`，包含全屏 image 对象、点击和左滑手势处理。接口遵循 `memory_watch_view` 模式，提供 create/destroy/get_screen/apply_model。已添加到 `main/CMakeLists.txt`。`idf.py build` 通过（app 11.1MB 剩 23%）。
- 实现 V1B `watchface_controller`：创建 `watchface_controller.h` 和 `watchface_controller.c`，包含状态轮询、动画更新和视图驱动。当前为 stub 实现，返回固定 idle 状态，动画循环播放。已添加到 `main/CMakeLists.txt`。`idf.py build` 通过（app 11.1MB 剩 23%）。
- 接入正式 UI 根首页导航：修改 `main/ui/custom/custom.c`，在 `custom_init` 中初始化表盘 view 和 controller，将表盘 screen 设置为活动 screen。点击表盘切换回原主界面。使用 LVGL 定时器轮询 controller。`idf.py build` 通过（app 11.1MB 剩 23%）。
- 运行 source tests：`test_watchface_anim_cache_source.py` 14 条通过，`test_watchface_resource_boundary_source.py` 2 条通过，`test_sd_manager_source.py` 2 条通过。
- 尝试 host preview 构建失败：`watchface_anim_cache.c` 和 `watchface_controller.c` 包含 ESP-IDF 头文件，host preview 环境缺少定义。需要条件编译。已部分修改，但仍有多个隐式函数声明错误。
- V1B 当前状态：watchface_view、watchface_controller、根首页导航已实现并编译通过。表盘可显示动画（stub idle 状态）。待修复 host preview 条件编译，待板端验证。
- 准备板端验证：固件已构建（app 11.1MB 剩 23%），可通过 `idf.py -p <PORT> app-flash` 烧录。需要 SD 卡已插入且包含 `/sdcard/watchface/*.rawanim` 文件。
- 修复 host preview 条件编译：移除 `watchface_controller.c` 中重复定义的主机 stub 类型/函数，改用真实头文件；调整 `watchface_anim_cache.c` 的 host 分支，避免与 mock `esp_err.h` 重复定义 `esp_err_t`/`ESP_OK`，并改用 mock 的 `esp_log.h`/`esp_timer.h`。host preview 构建成功，截图已生成于 `main/ui/agent_preview/artifacts/wifi-management-image-to-code.png`。`idf.py build` 与 source tests（18 条）仍通过。V1B 只剩板端验证待完成。
- 补充：发现 `custom_init` 在 firmware 与 host preview 中均未被调用，导致表盘未实际加载。已在 `main/ui/lvgl_task.c` 和 `main/ui/agent_preview/host_runner/main.c` 中于 `setup_ui` 后调用 `custom_init(&guider_ui)`。重新截图显示表盘白屏（host 无 SD 卡资源），确认表盘 screen 已作为根首页加载。固件与 host preview 构建均通过。
- 板端验证：用户烧录固件后启动日志显示 V1A 全链路正常运行。5 个 rawanim 文件全部探测成功（idle 12帧、thinking 8帧、working 12帧、message 12帧、error 10帧，均为 410x502 RGB565 @ 6fps）。PSRAM 帧槽分配 411640 bytes，帧读取约 800ms/帧。Wi-Fi、SD 卡、音频编解码器等硬件初始化正常。
- 修复 `watchface_controller_poll()` 性能问题：原实现每次 poll 都调用 `load_state`，导致每帧重复释放+分配 PSRAM 帧槽、重复读 SD 卡 header+frame table。修复为仅在状态切换时调用 `load_state`，后续帧只执行 `read_frame`。
- 修复编译错误：`watchface_controller_poll()` 中 `err` 变量作用域错误，已将声明提升到函数作用域。运行 `test_watchface_anim_cache_source.py` 14 条测试全部通过。`validate_context.py --level light` 上下文验证通过。
- 重新构建成功：`idf.py build` 通过，app 11.1MB，剩余 23%。
- 表盘帧率诊断与优化闭环：用户要求自行跑完全流程找出问题。通过 pyserial 直接读串口 45 秒，收集到 `[PERF]` 日志数据。根因确认：SD 卡每帧读取 778-781ms（SPI 10MHz + 每帧 fopen/fclose），有效帧率仅 1.28fps，远超 6ms 帧预算。实施三重优化：(1) `sd_manager.c` SPI 时钟 10→20MHz、DMA 块 4000→4096；(2) `sd_manager_read_range` 新增文件句柄缓存机制（`s_cached_file`/`s_cached_path`），同路径连续读取跳过 fopen；(3) `watchface_anim_cache.c` 改为 load_state 时一次性预加载全部 12 帧到 PSRAM（`s_frames_buffer`，~5MB），read_frame 零 SD 读取仅更新指针。实测结果：load_state 7.25s（一次性），read_frame 0ms，poll 0-1ms，有效帧率从 1.28fps 提升至 ~14fps（受 LVGL timer 70ms 限制）。PSRAM 占用 6341KB/8192KB (77.4%)。
- 清理活跃计划 `docs/context/plans/active/2026-06-30-watchface-emoji-root-ui-plan.md`：标记 V1 完成状态，更新"动画缓存策略"为最终"整段预加载到 PSRAM"方案，补充最终验证证据（780ms→0ms, 1.28fps→14fps, PSRAM 77.4%），将计划 status 从 active 改为 completed。
- 创建 V2 优化计划 `docs/context/plans/active/2026-07-03-watchface-optimization-v2-plan.md`：包含压缩优化、帧率提升、内存优化、功能增强和启动优化五个方向，目标 PSRAM 占用从 5MB 降至 2-3MB，帧率从 ~14fps 提升至 20-30fps，加载时间从 7.25s 降至 3-4s。
- SD 卡 SPI 频率阶梯测试：20MHz（当前）→ 25MHz 测试失败（`cmd=52 command CRC error`，挂载失败），确认 20MHz 是当前硬件可靠极限。已回退到 20MHz。
- `sd_manager.c` 新增 `SD_SPI_FREQ_KHZ` 可配置宏（默认 20000），`watchface_anim_cache.c` 的 `load_state` 日志增加吞吐量（KB/s）输出。这两个便利性改动保留。
- SD 读取软件优化测试：(1) `max_transfer_sz` 4KB→16KB 无明显效果；(2) 连续读取（1次 fseek+1次 fread 4.7MB）7145ms→7290ms 反而略慢。结论：FATFS 底层已优化连续扇区读取，瓶颈在 SPI 总线本身（20MHz 理论 2.5MB/s，实测 675KB/s，效率 26%）。两个优化改动已回退。
- 真正的优化方向：换 SDMMC 4-bit 模式（需硬件改造 +2 GPIO，理论 4 倍提升）、压缩帧数据（JPEG，需解码器 CPU 开销）、或接受当前 ~7 秒加载时间。
- 联网搜索 ESP-IDF SD 卡优化方案，总结如下：
  - **FATFS 配置优化**：当前配置 `CONFIG_FATFS_ALLOC_PREFER_EXTRAM=y`（优先使用 PSRAM）可能降低 I/O 性能，建议禁用以优先使用内部 RAM；`CONFIG_FATFS_PER_FILE_CACHE=y` 已启用；`CONFIG_FATFS_SECTOR_4096=y` 已启用；`CONFIG_FATFS_IMMEDIATE_FSYNC` 未设置（禁用），有利于性能。
  - **应用层优化**：尝试更大的 `max_transfer_sz`（如 32768/65536），确保缓冲区对齐（4 字节对齐），使用异步预加载（双缓冲）隐藏延迟。
  - **硬件层面**：切换到 SDMMC 4-bit 模式（需额外 2 个 GPIO），选择更快的 SD 卡（A2/U3/V30 等级），优化 PCB 布局（等长走线、去耦电容）。
  - **其他**：SD 卡速度等级影响巨大，劣质卡可能低于 5 MB/s；SDMMC 模式性能可达 SPI 模式的 2 倍以上。
- 测试 `CONFIG_FATFS_ALLOC_PREFER_EXTRAM=n`（禁用优先使用 PSRAM）：加载时间 7153ms，吞吐量 674 KB/s，与原始配置（7145ms）基本相同，无提升。已回退到原始配置。结论：瓶颈在 SPI 总线本身，FATFS 配置优化效果有限。
- **实现多重缓冲/流水线加载**：修改 `watchface_anim_cache.h/c` 新增 `load_state_metadata()`、`pipeline_start()`、`pipeline_tick()`、`pipeline_status()` 接口，实现双缓冲逐帧加载。修改 `watchface_controller.c` 使用流水线加载，状态切换时只加载元数据（header+frame table），立即加载第0帧并显示，后续帧通过 tick 逐步加载。修改 `custom.c` poll 间隔从 50ms 降至 20ms。
- **多重缓冲测试结果**：状态切换时间从 7145ms 降至 **645ms**（提升 11 倍），首帧显示延迟从 7145ms 降至 **645ms**。单次 tick 耗时约 7ms（读取 64KB），单帧加载时间约 49ms（7 次读取）。理论上可支持 20 FPS，但 SPI 总线带宽限制了同时加载多帧的能力。
- **多重缓冲技术细节**：使用双缓冲区（front/back buffer，各 411KB PSRAM），流水线加载时前台显示当前帧，后台逐步加载下一帧（每次 64KB）。帧加载完成后交换前后台缓冲区（零拷贝）。总 PSRAM 占用：2 × 411KB = 822KB（比之前 5MB 减少 83%）。
- **修复 rawanim frame_duration_us bug**：`pack_watchface_rawanim.py` 中 `int(round(1000 * fps))` 错误计算了帧持续时间（6 FPS → 6000 us = 6ms），修复为 `int(round(1000000 / fps))`（正确为 166667 us = 167ms）。同时修补了 5 个已有的 rawanim 文件 header。源动画全部为 6 FPS，低于用户要求的 10 FPS，需要更高帧率的源素材才能满足要求。
- **剩余瓶颈**：SPI 总线带宽限制（20MHz 理论 2.5MB/s，实测 675KB/s）。要达到稳定 10+ FPS 需要硬件改造（SDMMC 4-bit 模式）或数据压缩（JPEG）。
- **批量更新过时注释**：修复 watchface_anim_cache.c/h、watchface_controller.c、custom.c 中所有描述"一次性预加载"、"~5MB"、"100ms"、"780ms"、"V1A/V1B"等过时注释，统一反映流水线+双缓冲架构（双缓冲区各 411KB，总 822KB，单次读 64KB 约 7ms）。
- **实现 FreeRTOS 异步 SD 卡加载**：将 `pipeline_tick()` 中的阻塞 SD 卡读取改为后台任务异步执行。(1) 新增 `sd_load_task` FreeRTOS 任务（优先级 5，核心 1，栈 4096），通过 `sd_load_queue`（深度 2）接收加载请求；(2) 任务完成读取后通过 `xTaskNotifyGive()` 通知 LVGL 任务，`pipeline_tick()` 通过 `ulTaskNotifyTake(pdTRUE, 0)` 零阻塞检查；(3) `pipeline_start()` 发送第 0 帧请求到队列，不再阻塞当前 poll；(4) host preview 用 `#ifdef AGENT_PREVIEW_HOST` 保持同步兼容。效果：LVGL 线程 poll 阻塞从 ~7ms 降至 0ms，触摸响应延迟理论上降为 0。`idf.py build` 通过（app 11.1MB，剩余 23%）。
- **板端验证通过**（2026-07-03）：`idf.py -p COM3 app-flash` 烧录成功。切换控制台输出到 USB Serial/JTAG（CONFIG_ESP_CONSOLE_USB_SERIAL_JTAG=y），fullclean + rebuild 后再次烧录。串口日志确认：(1) LVGL poll 完全零阻塞（`poll=0 ms tick=0 ms`）；(2) `sd_load` 任务在独立核心运行（CPU 43%）；(3) 帧通过任务通知异步加载，全部 12 帧加载完成；(4) 单帧加载 800-1000ms（SD 卡 SPI 总线 675KB/s 带宽瓶颈，硬件限制）。
- **帧缓冲区数组重构**（2026-07-03）：将双缓冲方案替换为每帧独立 PSRAM 缓冲区数组（`s_frame_buffers[12]`，每帧 411KB，总计 ~4.9MB）。修改 `pipeline_start()` 同步加载第 0 帧并分配所有帧缓冲区，`pipeline_tick()` 检查后台任务完成状态并入队下一帧。`read_frame()` 支持两种模式：流水线模式从 `s_frame_buffers[]` 读取，一次性加载模式从 `s_frames_buffer` 读取。
- **控制器动画回放**（2026-07-03）：重写 `watchface_controller_poll()`，添加独立的动画回放循环。控制器按 `delay_ms`（167ms）间隔轮转已加载帧索引（`next_index = (current + 1) % loaded_count`），调用 `read_frame()` 更新帧描述符并应用到 view。解决了"所有帧加载完成后动画停止"的问题——现在动画在已加载帧范围内循环播放。
- **构建验证**：`idf.py build` 通过，app 11.1MB，剩余 23%。活跃计划已更新。