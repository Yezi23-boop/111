# 上下文库变更记录

- 2026-08-03：完成 `power_policy` 省电事实 Provider 注册制改造（`docs/context/plans/completed/2026-08-03-power-policy-fact-provider-refactor-plan.md`）。`power_policy` 新增固定容量 8 的静态省电参与者登记表，支持 facts-only / consumer-only / facts+consumer 三种形态：Safety Monitor 以 facts+consumer 接入（`must_keep_alive` → `BACKGROUND_CRITICAL`，预算变更唤醒 Safety policy task），`power_policy_audio_bridge.c` 以 facts-only 接入 `AUDIO_ACTIVE`（audio_codec 新增非阻塞 cached session snapshot + 会话变更回调，原有阻塞 getter 不进 policy task），runtime_coordinator 以内置 facts-only 从单一交接快照派生 `OTA_ACTIVE / PROVISIONING_ACTIVE`。`power_policy -> safety_monitor_policy` 的直接通知链已删除，改为注册式 `power_policy_budget_changed_notify()`（回调只唤醒 owner task，不在 policy 锁内执行）；provider 失败 fail-closed 为 `sleep_permission=NONE`。`system_time_service` 开始消费预算（STANDBY 下延后 SNTP 最多 30 秒后兜底同步）。注册必须在 `power_policy_start()` 前完成，`safety_monitor_policy_register_power_participant()` 在 app_main core policy 阶段调用。聚焦 source tests `31 passed`、全量 `503 passed`（5 个失败均为既有环境/字体脚本预存问题，与本改动无关）、`idf.py build` 通过、COM7 冷启动到 `network_service_ready` 且无 panic/WDT，三个 provider 注册顺序正确。未启用真实 sleep，STANDBY/dry-run 语义不变。

- 2026-08-03：新增并明确 `docs/context/plans/active/2026-08-03-power-policy-fact-provider-refactor-plan.md`：`power_policy` 使用一张 participant registry，同时支持事实提供和消费者 task 唤醒，不新增第二张唤醒表。Safety 以 facts+consumer 双角色接入；`audio_codec` 保持 AUDIO_ACTIVE 事实 owner，由窄 bridge 做映射，且必须先提供非阻塞 session snapshot；生命周期控制权、强前台交接、STANDBY 与 sleep dry-run 语义不变。

- 2026-08-02：补齐当前云端部署路由卡的香港服务器公网 IPv4 `103.42.182.35`，并把 `PANEL_HOST` / `IP 白名单` 纳入触发词与黄金查询，避免当前服务器 IP 事实只藏在 active 计划里。

- 2026-08-02：在线音乐首轮代码闭环完成：新增香港单容器 `music-service`、HTTPS 分块 MP3、单 FFmpeg/SQLite 播放会话、网易云私有 QR/来源适配、ESP32 PSRAM 流式播放器和独立音乐入口；服务端 Node 测试 9 项、ESP32 音乐 source tests 13 项、`idf.py build` 和 COM7 60 秒冷启动观察通过。香港远端部署、真实网易云扫码/播放、歌曲分页与 QR 位图 UI 仍待完成，不能视为 V1 全量验收。

- 2026-08-02：完成香港 1Panel 网站条目切换复核。`hermes-dashboard` 与 `watch-endpoint` 使用统一 `934000.xyz` 通配证书；停用并备份重复手工 vhost，将 watch 私有路径门禁固化到 `server/deploy/1panel/watch-endpoint/proxy/root.conf`。公网 runtime/private exposure、HTTP mock Ogg 和 WSS smoke 均通过。

- 2026-08-01：修复当前云端部署路由检索容易被旧 active plan / 历史 run 干扰的问题。新增 `current-cloud-deployment-route` 稳定知识卡，将 2026-07-13 香港中转记录标记为 `superseded`，`validate_context.py --level light --brief` 改为 all-scope brief，并为“当前/生产/现网/最新”查询增加稳定知识优先权与黄金查询回归。

- 2026-08-02：按要求将香港 Hermes 唯一管理入口切换到 1Panel「AI -> 智能体」。`Hermes-Agent` 应用现在使用生产 Mem0 派生镜像、`/opt/ai-memory-watch/hermes-data` 和两个私有 Docker 网络；watch/Relay Compose 不再启动 Hermes。切换后 1Panel 容器 healthy，公网 HTTP/WSS smoke、私有暴露门禁通过，旧 `ai-memory-watch-hermes` 容器已删除。

- 2026-08-02：收口 Hermes 外置 Mem0 香港迁移。用户取消 24 小时观察要求，香港三容器即时复测 healthy，公网 runtime/private exposure、HTTP mock Ogg 与 WSS smoke 通过；旧 Cloudflare Tunnel token 已轮换并停止旧 connector，阿里云旧应用容器已停止且取消自动重启，旧数据目录与迁移备份保留。香港生产仍固定 `MEM0_MODE=platform`、`MEM0_USER_ID=hermes-user` 与 `mem0ai==2.0.10`，未记录任何密钥或 token。

- 2026-08-02：完成 Hermes 外置 Mem0 香港迁移的首次公网闭环。香港使用 `1panel/hermes-agent:2026.7.20` 构建派生镜像 `ai-memory-watch-hermes:2026.7.20-mem0`，固定 `mem0ai==2.0.10`；`ai-memory-watch` 已通过 1Panel“路径选择”导入为 1Panel 来源编排，显示 `3/3` healthy。Hermes `/opt/data`、watch/Relay 数据目录均为香港独立新目录，Mem0 Platform 历史身份保持不变。`hermes.934000.xyz` 与 `watch.934000.xyz` 已切换香港 DNS-only A 记录，1Panel OpenResty 提供 Let's Encrypt HTTPS/WSS；watch 只代理 `/v1/watch/*`，8642/8787/9119 仍仅回环监听。公网 runtime/private exposure、HTTP mock Ogg 和 WSS smoke 均通过。计划：`docs/context/plans/completed/2026-08-02-hermes-external-mem0-hk-migration-plan.md`。

- 2026-07-31：完成 Runtime Coordinator 架构替换。注册式 FreeRTOS 控制面统一 Hermes、official chat、network provisioning、OTA 的 request/transition generation、deadline、ACK 与当前强前台事实；Safety Monitor 和普通 BLE presence 作为可抢占后台 participant，各业务 owner 仍独占真实资源生命周期。旧 `foreground_runtime_gate` 与旧 quiesce 协议已删除，`background_service_manager` 收敛为 `safety_monitor_policy`。聚焦 source tests `64 passed`；全量 pytest 为 `453 passed, 1 failed, 1 warning`，唯一失败是任务外已有 `danger_sample_recorder` reset-session 漂移；默认配置 `fullclean + build` 通过，`111.bin=0xab56b0`。COM7 模拟状态机及 Hermes/OTA 真实交接完成，默认固件冷启动到 `SERVICE_READY` 且无 panic/WDT。计划：`docs/context/plans/completed/2026-07-31-runtime-coordinator-plan.md`；证据：`docs/context/runs/2026-07-31-attempt-runtime-coordinator-board-handoffs.md`。

- 2026-07-31：将跨 owner 的运行时编排文件收敛到 `main/services/runtime/`：`foreground_runtime_gate`、runtime gate board test、`background_service_manager` 和 `startup_readiness` 不再分散在 `runtime_gate`、`safety`、`startup` 目录。Safety Monitor、OTA、网络、Hermes 与电源 owner 保持各自目录，运行时目录不接管资源生命周期。同步更新 CMake、include、source-test 路径、框架卡与活跃资源 gate 计划；聚焦 source tests `78 passed`，ESP-IDF build 通过，`111.bin=0xab5020`、最小 app 分区余量 `0x14afe0`（11%）。

- 2026-07-30：归档 Watch Foreground Session Lifecycle 计划。Hermes、official_chat、BLE provisioning 异步 owner、fail-fast foreground gate、5 秒切换等待和 generation-owned Safety quiesced ACK 的代码、测试、build 与 COM7 冷启动闭环已完成；真实 Wi-Fi 页面 BLE/SoftAP 交互和组合切换保留为后续独立真机验收，未在本文档中伪称已验证。计划：`docs/context/plans/completed/2026-07-14-watch-foreground-session-lifecycle-plan.md`。

- 2026-07-30：完成 V2.9 Relay 连接复用与 `watch-001` 单设备串行门禁云端验收。阿里云 Connector 私有 status 保持 `connected=true`、`handshake_count=1`、`disconnect_count=0`、无待重放；连续两个 Relay turn 完成，并发第二 turn 返回固定七字段 `relay_session_busy`，未进入 Hermes；`retryable` 门禁由本地测试锁定。公网 runtime/private exposure gate、HTTP mock smoke（`done/reminder_created`、7 字段）和 WSS smoke 均通过。期间修复云端 endpoint 镜像漏复制 `run_events.py` 与 `session_repo.py` 版本漂移，Hermes 未重启。证据：`docs/context/runs/2026-07-30-attempt-hermes-relay-v29-connection-reuse.md`；计划：`docs/context/plans/completed/2026-07-30-ai-memory-watch-hermes-v2.9-relay-reuse-serial-admission-plan.md`。

- 2026-07-30：实现 AI Memory Watch / Hermes V2.8 WSS 预连接与 Run Events 进度反馈。Hermes 页面前台由 `memory_watch_service` owner task 预连接并复用 WSS，离页关闭传输但不取消已接收 session；未建立连接且尚未发送音频时回退 HTTP multipart。ESP32 callback 改为 service-owned FreeRTOS queue，UI 只显示 `recognized/searching/executing/composing` 四个窄阶段，不接收原始 SSE、tool 名称或 token delta；进度队列非阻塞且为 ASR/最终回复/错误保留容量。Direct server 可选订阅 Hermes `/v1/runs/{run_id}/events` SSE，失败仍由既有 run polling 收敛；`WATCH_RUN_EVENTS_ENABLED=false` 默认关闭。`watch_session` 持久化最近阶段并支持 WSS 重连补发；Relay 未验证真实进度帧前不伪造搜索阶段。聚焦 source tests `46 passed`、server SSE/session/WSS tests `67 passed`，server 全量 `188 passed, 1 warning`，ESP-IDF build 通过（`111.bin=0xac0c20`，app 分区剩余约 23%）。执行计划：`docs/context/plans/active/2026-07-30-ai-memory-watch-hermes-v2.8-wss-preconnect-run-events-plan.md`。

- 2026-07-30：完成 V2.7 Hermes Relay 阶段 4 全链路验收。阿里云正式 Compose 部署 `watch-relay-connector` 到 `internal=true` 私网，持久 spool 与非 root UID 目录生效；先以 Direct 通过 runtime/private exposure/HTTP/WSS 门禁，再显式将新 `watch-001` session 灰度到 Relay。真实生产 canary 验证固定 session key、cancel、Hermes 停止期间 queued 与恢复 replay、HTTP/WSS 最终回复归属；Direct 回退演练通过后最终恢复 Relay。ESP32 COM7 已验证联网、health、inbox、真实按键 WSS 上传、离页 `/sync` running->done 和回复气泡；无 panic/Guru/stack overflow。期间修复 Relay HTTP compatibility waiter 的 `server_timeout` 误判，endpoint 聚焦测试 `20 passed`。另有一次本地录音 cleanup `ESP_ERR_INVALID_STATE`，未发服务器请求且后续录音恢复，列为后续独立优化项。证据：`docs/context/runs/2026-07-30-attempt-hermes-relay-stage4-production-canary.md`。

- 2026-07-30：完成 V2.7 Hermes `0.19.0` 阿里云私有网络 live canary。独立临时 Hermes、watch endpoint、Connector 验证了固定原生 session key `agent:main:relay:dm:watch-001`、普通/约 52 秒长任务最终回复归属、真实 cancel interrupt、Hermes 停止后的 SQLite spool queued 与恢复后的 replay；生产三个容器未重启、Direct 未切换。canary 发现并修复 Connector/endpoint bind mount 非 root UID 无法创建 SQLite、Connector 多源 Docker `COPY` 兼容和启动顺序注意事项。证据：`docs/context/runs/2026-07-30-attempt-hermes-relay-stage3-cloud-canary.md`。

- 2026-07-30：V2.7 Gateway Relay 已完成最终回复归属的服务器侧实现。由于 Hermes `0.19.0` 普通 Relay `send` 未稳定提供 `reply_to`，新增 Connector SQLite 持久 turn envelope：endpoint `request_id` -> `watch:{device}:{request_id}` inbound link，同一 `watch-001` 只允许一个 active turn；无 active/歧义/已完成 turn 的出站消息拒绝自动归属，明确但不匹配的 `reply_to` 也拒绝。新增 endpoint `relay_transport.py`、Connector spool 与内部 outbound 幂等回调，完成后不重放入站，网关断线取消保持 retryable。watch endpoint `179 passed`、Connector `11 passed`；Direct 仍为默认，Relay 尚未切入生产。执行计划：`docs/context/plans/active/2026-07-29-ai-memory-watch-hermes-v2.7-gateway-relay-watch-connector-plan.md`。

- 2026-07-30：V2.7 Gateway Relay 阶段 0 完成隔离 Hermes `0.19.0` canary。修复 Connector 与 Hermes 之间 newline-delimited JSON framing 后，`hello -> descriptor -> inbound -> typing/send -> outbound_result` 真实链路通过；但最终 `send` 未携带可信 `reply_to`/delivery link，因此暂停 endpoint 接入，Direct API 继续作为生产主路。证据：`docs/context/runs/2026-07-30-attempt-hermes-relay-stage0-wire-trace.md`。

- 2026-07-29：新增 `docs/context/procedures/hermes-cloud-docker-update.md`，固定 AI Memory Watch 云端 Hermes 的 Docker 镜像更新、健康验证与回退流程。Hermes 容器不支持 `hermes update`；正式路线是重建自定义镜像。默认基础镜像通过 `HERMES_BASE_IMAGE` 使用可达代理，构建参数可覆盖为已验证精确 tag；避免为单次镜像失败修改全局 Docker daemon 配置。

- 2026-07-29：将 IMU/Fall 默认关闭从编译期 `#if 0` 改为正常 service 生命周期调用。`imu_service` 新增异步 `imu_service_destroy()`，由 owner task 安全移除 GPIO ISR、释放任务和 PSRAM 缓冲；Fall 先销毁模型运行时，再销毁 IMU。动态重启需等待 IMU snapshot 回到 `STOPPED`，避免销毁中的任务与新实例并发。

- 2026-07-29：继续推进 Watch Foreground Session Lifecycle。将 foreground gate 收敛为明确的 `try_acquire(owner)` fail-fast API，移除无生产调用的 quiet window 与泛化 `FUTURE_PAGE` owner；修复 BLE/SoftAP provisioning 的强前台 gate ownership，SoftAP 启动前先停止普通 BLE presence，provisioning 退出后按 BLE 用户偏好恢复 presence；Wi-Fi 管理页在 provisioning 期间锁定断开/重试，避免 UI 线程直接触发重资源切换。聚焦 source tests `65 passed`，分层 warning `0`，ESP-IDF build 通过，`111.bin=0xabe1f0`、app free `0x341e10`/23%。真实 Wi-Fi 页面 provisioning 操作仍待 COM7 验证。执行记录：`docs/context/runs/2026-07-29-attempt-foreground-gate-and-ble-session-ownership.md`。

- 2026-07-29：为缓解 BLE provisioning 启动时 internal heap 连续块不足，将 Memory Watch / notification center 的大块业务缓存迁到 PSRAM。`memory_watch_service` 的 command/worker queue backing storage 与 conversation cache 改为 `heap_caps_calloc(..., MALLOC_CAP_SPIRAM | MALLOC_CAP_8BIT)`；Hermes UI controller 的 conversation/inbox/detail 缓存和全局通知中心 inbox scratch 也改为运行期 PSRAM 分配，避免 `.ext_ram.bss` 在当前 sdkconfig 未启用 external BSS 时仍落 internal RAM。验证：Memory Watch source tests `38 passed`；layering warning 0；`git diff --check` 无 whitespace error，仅 line ending warning；`idf.py build` 通过，`111.bin=0xabe230`、app free `0x341dd0`/23%；`idf.py size` 显示 DIRAM used `225147`、`.bss=44888`，相比本轮前约 `252355/.bss=72096` 释放约 27 KiB internal 静态占用；`nm` 仅剩相关缓存指针/小符号。执行记录：`docs/context/runs/2026-07-29-attempt-memory-watch-notification-psram-caches.md`。

- 2026-07-29：继续推进 Watch Foreground Session Lifecycle 阶段 3。Wi-Fi 管理页状态读取从直接调用有副作用的 `network_manager_get_status()` 改为读取 `network_service` 发布的纯 `network_service_wifi_status_t` 快照；`network_service_task` 仍是唯一轮询 manager 的 owner，UI timer 只复制缓存并渲染，避免页面刷新顺手推进底层 provisioning/runtime 状态。复查 subagent 发现旧 provisioning error 可能压过后续已连接文案，已让错误分支额外要求 `!status.wifi_connected`。验证：聚焦 BLE/network/UI source tests 34 passed；layering warning 0；全量 tests 431 passed / 5 个既有基线失败；`git diff --check` 无 whitespace error，仅 line ending warning；`idf.py build` 通过，`111.bin=0xabdf90`、app free `0x342070`/23%；COM7 `ble-provisioning-pure-wifi-snapshot` 30 秒 `panic_log_seen=false`，Wi-Fi 到 `SERVICE_READY`。阶段 3 仍缺真实 Wi-Fi 页面 BLE Provision 进入/退出与配网完成路径验证。

- 2026-07-29：新增 `AGENTS.md` 的 Agent 操作节流原则。默认按“最小实现 -> 聚焦验证 -> 批量收尾”工作，同一任务内多个小修复合并到一个验证批次；全量 pytest、`idf.py build` 和 `app-flash-monitor` 作为高成本验证，只在改动收敛、准备结案或低成本验证无法覆盖风险时运行；active plan、CHANGELOG 和 `runs/` 默认在一个可说明的小闭环结束后统一更新一次，板端验证应验证本轮真正改变的行为。

- 2026-07-29：收窄异常处理规则。`embedded-c-cpp-engineering-rules.md` 将“所有输入都检查、所有初始化/数据/超时/推理失败都必须降级”改为“只处理当前层负责、真实可能发生且失败代价明确的情况”；不负责恢复的错误应返回明确状态或交给 owner 处理，避免为假设场景堆叠保护和降级路径。

- 2026-07-29：继续收敛规则触发口径。`AGENTS.md` 与 `context-garden-policy.md` 明确 `CHANGELOG.md` 只记录后续有检索价值的摘要，不记录普通执行流水；`embedded-c-cpp-engineering-rules.md` 将长期 service/session 的 source test 触发从“必须锁定调用方向”收紧为“只有调用方向、owner 边界或板级事实属于 build 看不出的重要约定时才锁”。

- 2026-07-29：补充减少过度代码保护规则。`AGENTS.md` 嵌入式 C/C++ 默认规范明确：防护代码只覆盖真实可能发生、代价明确且当前层负责处理的失败；不要为假设中的极端场景堆叠保护、重试、状态机、兼容分支或包装层；能通过调用前置条件、owner 边界、build 失败或简单返回值检查解决的问题，不升级成复杂保护机制。

- 2026-07-29：优化 `AGENTS.md` 的测试触发口径。将“Build 用来证明代码能编译；测试用来证明重要约定没被破坏”收紧为“Build 是代码改动的默认验证；测试只用于保护 build 看不出的重要约定，不能因为改了代码就默认新增或扩展测试”，降低 agent 因普通代码改动频繁触发测试编写的倾向。

- 2026-07-29：收紧 `docs/context/runs/` 触发规则。`AGENTS.md` 明确 `runs/` 是反重复踩坑证据库，不是每次任务记录本；不再因为改动触碰 `FreeRTOS`、RAM/PSRAM、硬件驱动、分区表或 `sdkconfig` 就强制新建 run。只有大问题错误、路线取舍、被证伪尝试、板端特殊证据、跨 owner 决策或高成本验证结论，且未来能防止重复误判/重复排查时才写入 `runs/`；普通成功改动、常规 build 通过、小修小补和无复用价值过程不写。

- 2026-07-29：推进 Watch Foreground Session Lifecycle 阶段 3 局部闭环，将 Wi-Fi 管理页 BLE/SoftAP provisioning 入口从 LVGL 回调同步调用迁到 `network_service` 异步 owner worker。`network_service_request_ble()` / `network_service_request_portal()` 现在只提交请求并唤醒 owner；BLE provisioning start 在 internal-stack worker 中申请 `FOREGROUND_RUNTIME_OWNER_BLE_PROVISIONING` gate 后再调用 `network_manager_start_ble_provisioning()`；新增 `network_manager_stop_provisioning()` 与 `network_service_request_stop_provisioning()`，Wi-Fi 页面返回/删除时只提交异步 stop；`ble_presence_stop()` 等不到 host task 退出时改为 `ESP_ERR_TIMEOUT` fail closed，不再继续 `nimble_port_deinit()` 和清空 runtime。复查 subagent 发现自动结束 BLE provisioning 后切 SoftAP 可能覆盖 operation 并泄漏旧 gate，本轮补独立 `s_ble_provisioning_gate_held` 来源标记，周期对账和 SoftAP start 前释放均不再依赖最后一次 operation 类型。Wi-Fi 已连接且 BLE provisioning transport 仍 active 时，`network_service` 会自动投递 stop provisioning，由 internal-stack worker 完成 stop/deinit，避免 PSRAM 栈 monitor task 收尾 NimBLE/provisioning manager。`network_service_snapshot_t` 也新增 provisioning pending/error/generation，Wi-Fi 管理页可显示启动中/配网失败；旧 worker 完成时只有当前 generation 才能写 UI snapshot，避免覆盖新请求。验证：聚焦 BLE/network/UI source tests 30 passed；layering warning 0；全量 tests 431 passed / 5 个既有基线失败；`git diff --check` 无 whitespace error；`idf.py build` 通过，`111.bin=0xabde30`、app free `0x3421d0`/23%；COM7 `app-flash-monitor` 30 秒 `panic_log_seen=false`，Wi-Fi 到 `SERVICE_READY`。阶段 3 尚未完成，仍缺真实 Wi-Fi 页面 BLE Provision 进入/退出和配网完成路径验证。执行记录：`docs/context/runs/2026-07-29-attempt-ble-provisioning-async-ui-owner.md`。

- 2026-07-29：修复冷启动联网阶段 BLE transition worker 对空 network task handle 调用 `xTaskNotifyGive()` 导致的 `assert failed: xTaskGenericNotify tasks.c:5909 (xTaskToNotify)`。`network_service_task()` 入口现在立即发布 `xTaskGetCurrentTaskHandle()`，`network_service_finish_ble_transition()` 复制 notify target 后先判空再通知；极早完成的 worker 只记录 warning，不触发 FreeRTOS assert。验证：网络/BLE 相关 source tests 26 passed；`git diff --check` 无 whitespace error；`idf.py build` 通过，`111.bin=0xabd7a0`、app free `0x342860`/23%；COM7 `app-flash-monitor` 30 秒 `panic_log_seen=false`，日志显示 `BLE transition complete` 后继续启动到 IMU/Fall disabled by default。执行记录：`docs/context/runs/2026-07-29-attempt-network-ble-transition-null-notify.md`。

- 2026-07-29：按用户要求让 IMU/Fall 后台链路开机默认关闭，并关闭周期 INFO 刷屏。`app_main.c` 中 `imu_service_start()` 与 `fall_detection_service_start()` 现在位于受控 `#if 0` 调试块内，默认只打印 `boot_stage: imu_service_disabled_by_default` / `fall_detection_disabled_by_default`；`qmi8658c` 的 `原始表`、`imu_service` 的 `采样表`、`wakeup_evidence` 的 `rtc_int_sample` 和 `sleep_coordinator` 的 `dry_run` 均从 `ESP_LOGI` 降为 `ESP_LOGD`。验证：相关 source tests 44 passed；`git diff --check` 无 whitespace error；`idf.py build` 通过，`111.bin=0xabd730`、app free `0x3428d0`/23%。执行记录：`docs/context/runs/2026-07-29-attempt-imu-default-off-quiet-periodic-logs.md`。

- 2026-07-29：将 `watch.934000.xyz` 主路从香港 relay connector 切到阿里云本机 cloudflared connector，并修复 MiMo 按量付费 API 配置。阿里云 `ai-memory-watch-cloudflared` 已成功注册到 `ai-memory-watch` Tunnel；为兼容当前 Cloudflare ingress，云端 Compose 额外绑定 `127.0.0.1:18787 -> watch endpoint:8787` 与 `127.0.0.1:19119 -> Hermes Dashboard:9119`。删除香港 `ct-23` connector 后，公网 `runtime_status.ps1 -BaseUrl https://watch.934000.xyz -AssertPrivateNotExposed` 通过，私有 `/health`、`/v1/models`、`/v1/responses`、`/internal/watch/inbox` 均未暴露；公网裸 health 返回 `401` 为缺 device token 的预期鉴权结果，不再是 `502`。MiMo `sk-` 接入密钥需配 `https://api.xiaomimimo.com/v1`；Hermes 持久 `.env` 与 watch endpoint 独立 ASR secret 都必须同步，缺任一处都会导致文本恢复而真实语音 ASR 失败。三处配置同步并重建 endpoint 后，Hermes `/v1/responses`、公网 runtime gate、HTTP mock Ogg、WSS smoke 以及 HTTP/WSS 真实 Ogg Opus ASR smoke 均通过。

- 2026-07-20：完成 Hermes 云端内存与香港 relay 自愈加固。阿里云上一启动周期在 watch/Hermes/SSH 同时失去响应前持续记录 `Under memory pressure`，但没有 kernel panic、磁盘满或明确 OOM kill；云端 Compose 现将 Hermes/watch endpoint 限制为 `768m/256m` RAM、`1024m/384m` memory+swap 和 `256/128` PID，并增加日志轮转、Hermes healthcheck 与默认禁用阿里云旧 cloudflared profile。阿里云和香港分别部署 30 秒 health timer：前者记录容器内存并在连续 3 次失败后重启单容器，后者在双 upstream 或 listener 失效时重建 autossh。受控 relay 停止演练自动恢复，公网 runtime gate 与 WSS smoke 通过。执行记录：`docs/context/runs/2026-07-20-attempt-hermes-cloud-memory-relay-hardening.md`。

- 2026-07-19：将 Fall 2s CNN 从报警决策者改为候选生成器。`imu_service` 事件窗口扩为 50Hz / 6s / 300 帧，事件点固定第 50 帧；`fall_detection_fill_model_input()` 只提取 `[trigger-35, trigger+64]` 的 100 帧 2s 子窗口，模型输入仍为 `[1,600]` 且 gyro 保持 `deg/s -> rad/s`；`fall_detection_service` 新增 post-check，普通确认要求 `fall_prob>=0.60 && low_motion && posture_change`，强置信兜底要求 `fall_prob>=0.90 && low_motion`。post-check 失败时只记录 candidate/post 检查日志，不红屏、不上传。验证：source tests 17 passed；`git diff --check` 无 whitespace error；`idf.py build` 通过，`111.bin=0xac6e80`、app free `0x339180`/23%；context standard 错误 0、警告 0。执行记录：`docs/context/runs/2026-07-19-attempt-fall-candidate-post-check.md`。

- 2026-07-19：进一步优化 Fall candidate + post-check 的边界保护和日志可解释性。`fall_detection_service` 新增窗口契约校验，推理前检查基础窗口、模型 100 帧子窗口和 post-check 区间是否同时成立；`post检查表` 增加触发帧 `trigger_acc/trigger_gyro/trigger_jerk`，便于板端对照误触发来源。验证：source tests 17 passed；`git diff --check` 无 whitespace error；`idf.py build` 通过，`111.bin=0xac6f40`、app free `0x3390c0`/23%；context standard 错误 0、警告 0。执行记录仍归入 `docs/context/runs/2026-07-19-attempt-fall-candidate-post-check.md`。

- 2026-07-19：收紧 Fall 2s CNN 事件触发入口并恢复测试契约。`imu_service_event_flags()` 不再允许 `jerk` 单独触发事件窗口，必须同时满足 `acc_norm>16.0m/s^2` 或 `gyro_norm>3.0rad/s`；当前事件阈值锁定为 `A_high=25.0m/s^2`、`G_high=5.0rad/s`、`J_high=10.0m/s^2/frame`、`GJ_min=5.0m/s^2/frame`，模型确认阈值锁定为 `FALL_MODEL_THRESHOLD_DEFAULT=0.60f`。同步修复 fall/imu source tests 的旧阈值断言。验证：fall/fall_inference/imu source tests 17 passed；`git diff --check` 无 whitespace error；`idf.py build` 通过，`111.bin=0xac6640`、app free `0x3399c0`/23%。执行记录：`docs/context/runs/2026-07-19-attempt-fall-jerk-trigger-tighten.md`。

- 2026-07-14：完成 official_chat 前台 owner 生命周期收敛。enter 命令现在先申请 foreground gate，失败时 fail closed，不创建 `s_chat_handle`；停机顺序改为 prepare/stop、idle + transport quiet period、callback 解绑、instance/audio/transport destroy、清除前台 audio blocker，最后 release gate。release 失败保持 held 与 shutdown pending，不伪装 STOPPED；底层 start 失败也进入统一 shutdown 释放路径。聚焦 tests `28 passed`；ESP-IDF build 通过，`111.bin=0xac6040`、app 余量 23%。执行记录：`docs/context/runs/2026-07-14-attempt-official-chat-foreground-owner-lifecycle.md`。

- 2026-07-14：完成 Memory Watch 前台 owner 生命周期阶段。`memory_watch_service_set_foreground()` 现在只记录 desired state/generation 并唤醒 owner task，foreground gate 的 acquire/release 收回 service task；snapshot 增加 foreground state/READY/error/generation，录音入口在 gate 未 READY 时拒绝。离页会等待 upload worker 结束 recorder/WS operation 后再 release gate，5 秒超时保持 fail closed；初始化改为 worker 先创建、service task 最后发布，部分失败统一回滚 task、queue/event 和 conversation PSRAM staging。Inbox、pending `/sync` 和最近对话缓存继续后台保留。聚焦 tests `44 passed`，全量 `422 passed / 7` 个既有失败；ESP-IDF build 通过，`111.bin=0xac5f30`、app 余量 23%。执行记录：`docs/context/runs/2026-07-14-attempt-memory-watch-foreground-owner-lifecycle.md`。

- 2026-07-14：新增 `plans/active/2026-07-14-watch-foreground-session-lifecycle-plan.md`，固定强前台资源治理的执行顺序：先由 Hermes、official_chat、BLE provisioning 各自 service owner 建立幂等 create/stop/destroy、desired state、generation 和 READY/STOPPED snapshot，再接 foreground gate、旧 owner 5 秒有序切换和 Safety Monitor quiesced ACK。用户已确认主动切换可自动关闭旧 session，5 秒超时后 fail closed 且不强制 release/delete/reboot；后台 HTTPS 继续 owner 自治，不新增中央 ResourceManager。初步计划分 0-7 阶段，下一步先做只读资源清单。

- 2026-07-14：撤除 `background_https_gate` 共享 token 与 quiet window。删除 gate 实现/CMake/source test，Memory Watch health/alert/inbox/mark-read/sync 和 weather 恢复为 owner 自治，official_chat 与 BLE 不再打开后台 quiet window；保留 foreground 强前台互斥、Safety Monitor/ESP-DL 让路、BLE 单次延迟重试与 fail closed，以及 Memory Watch pending/due 瞬时错误恢复。同步重写资源报告，明确后台请求允许自然并发，若出现可重复 TLS/`NO_MEM` 优先修具体 owner，不恢复中央 job queue。聚焦 source tests `63 passed`；全量 `420 passed / 7` 个任务前既有失败；ESP-IDF fullclean/build 通过，`111.bin=0xac5a60`、app 余量 23%。无 gate 真机高压回归保留在 active plan 阶段 6。执行记录：`docs/context/runs/2026-07-14-attempt-remove-background-https-gate.md`。

- 2026-07-14：完成 Watch 工程代码目录按 owner 整理。host preview 迁到 `tools/ui_preview`；Memory Watch、power、network、sensor、runtime gate、startup、time、weather、safety、audio diagnostic 收敛到窄 service 子目录；主屏电量/天气刷新从 generated UI 移到 custom runtime；天气拆成 snapshot service + DTO HTTP client。两名 subagent 复查无 P0，并修复 DTO reentrancy、mutex 分配失败保护和本地敏感 header ignore。分层 strict 检查 `warning_count=0`，聚焦 source tests `79 passed`，server `174 passed`，ESP-IDF 5.5.3 fullclean/build 通过，最终 `111.bin=0xac5f30`、app 余量 23%。全量 source tests 的 7 个既有漂移未混入修复。执行证据：`docs/context/runs/2026-07-14-attempt-watch-code-directory-reorg.md`。

- 2026-07-14：完成 AI Memory Watch / Hermes V2.5 审查修复。server 增加 terminal session reply replay、Hermes run GET poll 瞬时重试、启动未知态 `interrupted`、持久化 run deadline/clarification、重连后动态 WS 投递和统一 runtime readiness；Hermes `0.18.2` `/v1/runs` 未实现幂等键，故不重试不确定的启动 POST。ESP32 使用 FreeRTOS EventGroup 消费 `request_accepted`，仅在 server 确认后转后台 pending；公网私有 gate 改用真实 POST 探测并删除 device token 的 Inbox create 入口。watch endpoint 同时固定基础镜像 digest、恢复 pip TLS 校验并以非 root/read-only/cap-drop 运行。验证：server `174 passed`、ESP32 source `55 passed`、ESP-IDF build、公网 private gate 与 WSS mock Ogg 通过；云端 SQLite integrity `ok`、active/duplicate 均为 0。执行证据：`docs/context/runs/2026-07-14-attempt-hermes-v25-review-fixes.md`。

- 2026-07-14：执行 AI Memory Watch / Hermes V2.5 对话可靠性计划。正式 WSS 主链路已切换到 Hermes `0.18.2` 原生 `/v1/runs`，持久化 `hermes_run_id`，实现 SQLite 原子 claim、conversation role 唯一约束、terminal replay、同设备串行、取消/完成单终态和 restart `interrupted` 边界；ESP32 在已收到 ASR 后的本地 WS timeout 改为后台 pending，并按 UTF-8 字节边界安全截断。Inbox 写入迁到独立 `/internal/watch/inbox` credential，公网 device token 默认只读；WS health 指标覆盖 upload/ASR/queue/Hermes/persist/delivery。server `161 passed`、ESP32 source `55 passed`、ESP-IDF build、公网 private gate、HTTP mock Ogg 与 WSS smoke 均通过；阿里云 schema migration 后 active session 与 duplicate group 均为 0。Dockerfile 同时将 pip 固定到阿里镜像，依赖安装由默认源近 4 分钟无响应降到约 19 秒。执行证据：`docs/context/runs/2026-07-14-attempt-hermes-v25-conversation-reliability.md`。

- 2026-07-13：完成 AI Memory Watch 香港 NAT 中转与可信 TLS 公网闭环。256 MiB Debian 12 LXC 无 `/dev/net/tun` 且无 `CAP_NET_ADMIN`，因此使用阿里云受限 `watchrelay` SSH key + 香港 autossh + nginx-light upstream keepalive；Cloudflare 新增 DNS-only `watch-hk.934000.xyz` A 记录，Let's Encrypt DNS-01 ECC 证书签发成功，acme.sh `3.1.4` + Debian cron 已自动续期。公网 runtime gate、mock Ogg、text、WebSocket smoke 均通过，固定 7 字段与无效 token 403 符合契约，私有路径均 404。可信 HTTPS watch health 7 次中位 `0.382s`、平均 `0.462s`，比 Cloudflare HTTP/2 中位 `0.765s` 低约 50%；`nginx`、`cron`、`watch-relay-autossh` 均 active，available memory 约 224 MiB。正式 `watch.934000.xyz` Tunnel 保持不变，香港入口为 `https://watch-hk.934000.xyz:20340`。attempt log：`docs/context/runs/2026-07-13-attempt-hk-watch-relay.md`。

- 2026-07-13：完成 AI Memory Watch / Hermes 阿里云云端副本部署。Ubuntu 24.04 2C2G 主机已安装 Docker/Compose、增加 2 GiB swap，并由 `compose.cloud.yml` 管理 Hermes、watch endpoint 与 cloudflared；`8642/9119/8787` 均只绑定服务器 `127.0.0.1`，Cloudflare 公网仍只转发 `/v1/watch/*`。修复 Docker Hub 拉取超时、watch endpoint Debian APT 切换阿里源、Hermes `mem0ai` 懒安装导致 `/v1/responses` 超过 115 秒，以及 cloudflared token UID 权限问题。验证：Hermes `0.18.2` health/models/responses 通过；公网 runtime gate 与 mock Ogg/text smoke 通过；私有路径均为 404；固定 7 字段和 403 鉴权失败符合契约。attempt log：`docs/context/runs/2026-07-13-attempt-hermes-aliyun-cloud-deploy.md`。

- 2026-07-08：部署 Fall 2s/6ch CNN recall90 调试模型。`components/fall_detection_inference/models/esp32s3/` 只保留 `cnn_v1_recall90_6ch_2s_with_test.espdl`，SHA256=`cbe18c7e089bac506ff5229f2ed8c4b728148df5902dce9527aad3a315504684`；`fall_model_runner` 输入契约改为 `[1,600]`，默认阈值改为 `0.30`；`imu_service` 事件窗口改为 50Hz / 2s / 100 帧，event 前 35 帧 + 后 65 帧；gyro 仍在 `fall_detection_fill_model_input()` 中由 `deg/s` 转 `rad/s`。验证：fall/fall_inference/imu source tests 17 passed；`git diff --check` 无 whitespace error；`idf.py build` 通过，`111.bin` `0xac65e0`，app free `0x339a20`/23%。当前机器仅发现 `COM1`，未执行板端 app-flash/monitor；attempt log：`docs/context/runs/2026-07-08-attempt-fall-event2s-cnn-recall90-deploy.md`。

- 2026-07-08：修复 AI Memory Watch 后台 HTTPS gate 忙时的 health/inbox 重试语义。`memory_watch_service` 现在对后台 health 增加 worker busy/pending/retry due，瞬时失败不再立刻把 `hermes_online` 置为 false；inbox poll 失败后保留 pending 并按 5 秒退避重试，auth/protocol 错误才暂停，避免 `background https busy -> failed -> dispatch` 紧循环。验证：`tests.test_memory_watch_service_source` 20 passed；相关 gate/source tests 27 passed；`git diff --check` 无 whitespace error；`idf.py build` 通过，`111.bin` `0xace350`，app free `0x331cb0`/23%。attempt log：`docs/context/runs/2026-07-08-attempt-memory-watch-background-https-retry.md`。

- 2026-07-08：为 Fall RF5s 运行时新增对外一键销毁入口。`fall_detection_service_destroy()` 会先断开 `imu_service` 的 fall 窗口队列但保留 IMU 后台采样；运行中的 `fall_detect` task 进入 `STOPPING`，在安全点清理跌倒本地告警、释放 ESP-DL runner、static queue、queue storage、current window 和 model input PSRAM 缓冲后标记 `STOPPED`。启动失败路径也复用资源释放逻辑，减少模型/窗口缓冲残留。验证：fall/fall_inference/imu source tests 17 passed；`git diff --check` 无 whitespace error；`idf.py build` 通过，`111.bin` `0xace0b0`，app free `0x331f50`/23%。attempt log：`docs/context/runs/2026-07-08-attempt-fall-runtime-one-click-destroy.md`。

- 2026-07-08：修复 official_chat AI 页面首次按住说话才连接 WebSocket 导致按钮消失的问题。新增 `official_chat_prepare_audio_channel()` / `official_chat_is_audio_channel_ready()`，service snapshot 增加 `audio_channel_ready`，AI 页面进入后在 `IDLE && !audio_channel_ready` 时自动预连接 WebSocket/音频通道；按钮仅在通道 ready 后显示。根据真机 `esp-aes: Failed to allocate memory` 日志，追加 official_chat 前台 runtime gate owner、后台 HTTPS 8 秒 quiet window，并将 UI 自动预连接重试节流到 15 秒。验证：相关 source tests 38 passed；`idf.py build` 通过，`111.bin` `0xacdeb0`，app free `0x332150`/23%。attempt log：`docs/context/runs/2026-07-08-attempt-official-chat-ws-preconnect.md`。

- 2026-07-08：收紧 `AGENTS.md` 的测试生成口径。`Goal-Driven Execution` 从默认“写测试复现”改为按风险选择最小验证：局部 C/C++ 修复、注释/拼写、简单参数和不改变 API/owner/并发/内存归属的改动默认 build-only；已有测试能覆盖时只补最小断言；只有 build 看不出的契约、历史大回归、跨层边界、FreeRTOS/PSRAM/硬件 owner 或持久化 schema 风险才新建 Python/source test；禁止为了证明小改动而写只复述实现细节的 regex test。

- 2026-07-08：清理 Fall Detection 旧模型资产，只保留当前 RF5s 模型。`components/fall_detection_inference/models/esp32s3/` 删除旧 `cnn_c24_pool225_do015_e80_with_test.espdl` 和 `tcn_v1_rf4s_6ch_5s_with_test.espdl`，保留当前 CMake 嵌入的 `tcn_v1_rf5s_6ch_5s_with_test.espdl` 及 meta；source test 锁定目录内只能有这一份 `.espdl`。RF5s meta 的当前固件阈值同步为 `0.65`，并保留训练推荐 `0.85` 作为说明。验证：fall source tests 16 passed；RF5s meta JSON 解析通过；`idf.py build` 通过，`111.bin` `0xad3d30`，app free `0x32c2d0`/23%。

- 2026-07-08：调低 Fall RF5s 默认告警阈值用于更容易触发跌倒链路。`FALL_MODEL_THRESHOLD_DEFAULT` 从 `0.85` 改为 `0.65`；Event Trigger 阈值不变，模型仍为 `tcn_v1_rf5s_6ch_5s` / `[1,1500]`。这是调试期提高召回和告警触发率的设置，会提高 ADL 误报风险；后续量产阈值需结合板端 ADL/FALL 日志回调。验证：fall source tests 16 passed；`idf.py build` 通过，`111.bin` `0xad3d30`，app free `0x32c2d0`/23%；COM7 `app-flash-monitor` 显示 `threshold=0.65`、`Model: Test Pass!`、`panic_log_seen=0`；context standard 错误 0、警告 0。

- 2026-07-08：修复 Fall RF5s 背面朝上误告警路径并限制本地告警时长。`imu_service` 删除 `flags=0` 定期窗口发布，模型只接收 Event Trigger 后的 50Hz / 5s / 250 帧窗口；`fall_detection_service` 拒绝非事件窗口，确认后本地红屏/告警最多保持 5 秒并自动 clear，仍保留一次 App `danger_type="fall"` 上传且不播放危险提示音。原因是板端日志显示 `flags=0` 定期窗口曾给出 `fall_prob=0.9997` 并直接确认 FALL，绕过 V1 事件触发设计。验证：source tests 16 passed；`idf.py build` 通过，`111.bin` `0xad3d30`，app free `0x32c2d0`/23%；context standard 错误 0、警告 0；COM7 `app-flash-monitor` 无 panic，`定期窗口表=0`，fall/imu `flags=0x00=0`，确认后约 5 秒 clear。attempt log：`docs/context/runs/2026-07-08-attempt-fall-remove-periodic-window-5s-clear.md`。

- 2026-07-08：固定跌倒告警默认静音策略。`fall_detection_service` 继续在确认跌倒后上传 `danger_type="fall"` 的 `danger alert`；`app_alert_manager` 对 `APP_ALERT_SOURCE_FALL_DETECTION` 保留本地屏幕/震动告警，但跳过 `audio_alert_player` 危险提示音播放，也不抢占普通音频输出。验证：相关 source tests 33 passed；`idf.py build` 通过，`111.bin` `0xae0cb0`，app free `0x31f350`/22%。attempt log：`docs/context/runs/2026-07-08-attempt-fall-danger-alert-silent-audio.md`。

- 2026-07-08：完成 Fall Detection V1 第一阶段 Event Trigger + 5s 事件窗口。`imu_service` 从每约 2 秒发布任意 4 秒加速度滑窗，改为逐帧计算 `acc_norm/gyro_norm/jerk_norm`，仅在高动态事件触发并收满 50Hz / 250 帧窗口后发布事件窗口；窗口 payload 保留 acc+gyro 物理量和 trigger 元数据。`fall_detection_service` 暂不替换 `.espdl`，旧 3ch / `[1,600]` 模型仅作为事件窗口后的 legacy 兼容消费者，避免静止任意滑窗继续直接进入推理。验证：目标 source tests 13 passed；`idf.py build` 通过，`111.bin` `0xae0bd0`，app free `0x31f430`/22%。attempt log：`docs/context/runs/2026-07-08-attempt-fall-event-trigger-5s-window.md`。

- 2026-07-08：修正 ESP-IDF `export.ps1` 路径表述。当前机器确认优先路径为 `D:\esp-idf\v5.5.3\esp-idf\export.ps1`；根 `AGENTS.md` 与 `agent-operational-rules.md` 明确禁止假设仓库根目录 `D:\esp32S3\111\export.ps1` 或 `D:\esp32S3\esp-idf\export.ps1` 存在；历史验证记录中的 `. export.ps1` 短写改为完整路径，避免误导后续 agent。

- 2026-07-08：修复 Fall Detection V1 active plan 的 light 检索与文档契约。`validate_context.py --level light` 现在按 `runs -> plans -> mixed` 顺序检索，避免 active plan 因默认 mixed 不包含 `plans/` 而漏召回；`docs/context/plans/active/FALL_DETECTION_V1_111_DEPLOYMENT_DESIGN.md` 补齐 frontmatter、`## 进度`、当前实现差距和下一步，明确当前旧 `3ch/4s/[1,600]` 连续滑窗直接告警不等于 V1 目标态；`query-golden.yaml` 新增 Fall V1 plans 检索回归。

- 2026-07-07：完成 IMU/Fall 跌倒检测链路 internal RAM 回收。将 `imu_service` 的 200 帧 ring 与 publish window、`fall_detection_service` 的 queue storage/current window/model input 从静态 internal RAM 改为 `heap_caps_*` + `MALLOC_CAP_SPIRAM | MALLOC_CAP_8BIT`；`fall_detect` task 栈改为 `xTaskCreateWithCaps(..., MALLOC_CAP_SPIRAM)`，小状态/锁/句柄继续留 internal。验证：相关 source tests 62 passed；`git diff --check -- . ':!managed_components'` 无 whitespace error；`idf.py build` 通过，`111.bin` `0xae07a0`；`size-files` 显示 `imu_service.c.obj ram_st_total=174`、`fall_detection_service.c.obj ram_st_total=224`、`.espdl.S.obj ram_st_total=0`；COM7 `app-flash-monitor` 日志 `board_logs/2026-07-07-07-46-41-fall-psram-window-buffers.log` 显示 `internal_free=26482 largest=24576`，相比上一轮 `internal_free=1142 largest=672` 明显恢复，同时 `window_published`、`fall_window_result`、`fall_alert_confirmed`、本地震动/提示音、App 上传和 `fall_alert_cleared` 均正常，`panic_log_seen=false`。attempt log：`docs/context/runs/2026-07-07-attempt-fall-imu-psram-window-buffers.md`。

- 2026-07-07：完成 ESP-DL IMU 跌倒确认告警状态机。`fall_detection_service` 新增 `IDLE/FALL_CONFIRMED` 状态：单窗口 `fall_prob>=0.80` 即确认跌倒并触发一次 `app_alert_manager` 本地完整告警和一次 `watch_endpoint_service_post_danger_alert(danger_type="fall", message="检测到跌倒")`；确认期间不重复告警/上传，连续 3 个 `fall_prob<0.50` 后 clear。`app_alert_manager` 增加 `APP_ALERT_SOURCE_FALL_DETECTION` / `APP_ALERT_LABEL_FALL` 和中文标签 `跌倒`。板测发现 haptic/audio 临时 task 默认栈会受 internal RAM 压力影响并报 `ESP_ERR_NO_MEM`，已将两个 alert player 的 task 栈改为 `xTaskCreateWithCaps(..., MALLOC_CAP_SPIRAM)`。验证：目标 source tests 62 passed；`git diff --check -- . ':!managed_components'` 无 whitespace error；`idf.py build` 通过，`111.bin` `0xae4420`，app free `0x31bbe0`/约 22%；COM7 `app-flash` hash verified；串口 `board_logs/2026-07-07-07-26-15-fall-alert-state-machine-psram-alert-tasks.log` 显示 `fall_alert_confirmed`、红屏、震动 started/finished、提示音 started/finished、`danger alert dispatched: type=fall` 和 `fall_alert_cleared`，`panic_log_seen=false`。attempt log：`docs/context/runs/2026-07-07-attempt-fall-alert-state-machine.md`。

- 2026-07-07：新增 `docs/context/plans/completed/2026-07-07-watch-code-directory-reorg-execution-plan.md`（当时位于 active，现已归档），记录工程代码目录整理执行计划。当前决策为保留 ESP-IDF 根工程结构和单 `main` component；用 owner 边界区分 `features` 与 `services`；Memory Watch 拆成 `features/memory_watch` 产品语义、`services/memory_watch` 后台 runtime、`main/ui/custom` 页面三层；`main/ui/agent_preview` 后续迁到 `tools/ui_preview`；`main/app/board_*`、`components/*`、`server/` 和产物目录暂不混入固件源码整理。

- 2026-07-07：完成自训练 ESP-DL IMU 跌倒模型部署闭环。模型资产锁定为 `components/fall_detection_inference/models/esp32s3/cnn_c24_pool225_do015_e80_with_test.espdl`，SHA256=`10526143f02d047b0e5b2c29f29802396171998cfb4071cb54d7858375a98d54`，契约为 `FLOAT [1,600]`、50Hz × 4 秒、三轴加速度 `m/s^2`、逐帧交错 `accX/accY/accZ`，输出 `[ADL,FALL]` 且模型内已有 Softmax。新增 `components/fall_detection_inference` 封装 ESP-DL loader/self-test/run；`imu_service` 新增 200 帧窗口副本队列出口；`fall_detection_service` 消费窗口、做 `[-x,+y,-z]` 轴向映射、推理并只发布日志/snapshot，不接 UI、不触发告警。验证：source tests 29 passed；`git diff --check -- . ':!managed_components'` 无 whitespace error；`idf.py build` 通过，`111.bin` `0xae3f90`，app free `0x31c070`/约 22%；`idf.py -p COM7 app-flash` hash verified；串口 `board_logs/2026-07-07-06-39-36-fall-detection-espdl.log` 显示模型加载、`dl::Model: Test Pass!`、50Hz 采样、窗口发布和连续 `fall_window_result`，`panic_log_seen=false`。attempt log：`docs/context/runs/2026-07-07-attempt-fall-detection-espdl-imu-deployment.md`。

- 2026-07-07：记录当前板 IMU 六面安装方向。基于用户提供的六面测试成功日志，`board_imu.h` Doxygen 现在锁定：表盘朝上/正面朝上 -> QMI8658C `-Z`，表背朝上 -> `+Z`，手表右侧/USB 侧朝上 -> `-X`，手表左侧朝上 -> `+X`，手表顶部朝上 -> `+Y`，手表底部朝上 -> `-Y`。该映射作为板级安装事实记录，不下沉到 `qmi8658c driver`，也不直接等同于后续 fall/raise 模型输入坐标系。同步更新 QMI8658C knowledge 和 IMU active plan，source test 锁定映射文本。

- 2026-07-07：按用户确认先做稳定 50Hz IMU 采样。当前链路保持 `imu_service -> imu_sensor -> qmi8658c`，`board_imu` 只提供板级事实；`imu_service` 默认启动后安装 GPIO21 ISR，并用 `vTaskDelayUntil` 每 20ms 读取一次物理六轴，维护 200 帧 / 4 秒环形缓冲，snapshot 发布 `sampling_active/sample_count/sample_error_count/sample_window_ready/last_sample_interval_us`。QMI8658C 读取方法按当前已验证口径修正为 `STATUS0 -> TIMESTAMP -> TEMP -> AX_L 12 bytes`，配置 `CTRL1=0x60`、`CTRL5=0x03`，避免旧连续读法导致样本窗口对齐异常。验证：IMU/QMI source tests 24 passed；`git diff --check -- . ':!managed_components'` 仅 LF/CRLF warning；`idf.py build` 通过，`111.bin` `0xac7780`，app free `0x338880`/23%；`idf.py -p COM7 app-flash` 通过并完成 hash 校验；串口 `board_logs/2026-07-07-06-11-28-serial.log` 显示 `sampling_started: rate_hz=50 window_frames=200`、`sample_50hz count=200 window_ready=1`，持续采到 `count=1350`，`panic_log_seen=0`。attempt log：`docs/context/runs/2026-07-07-attempt-imu-service-50hz-sampling.md`。

- 2026-07-07：新增 `docs/context/knowledge/project/watch-device-board-layering-review.md`，沉淀对当前设备层与板层的专项审查。结论为设备/板层没有 P0 级结构错误，AXP2101 -> board_power -> power_service 三段较清楚；但 `imu_service` 仍直接依赖 `qmi8658c`，与推荐的 `imu_service -> board_imu -> qmi8658c` 链路错位；QMI8658C driver 仍有默认地址路径；`axp2101_read_snapshot()` 内部会受控启用 ADC channel，不能当纯 getter；`board_ds2413_motor` 是偏厚的板级设备 owner；`i2c_manager` 与部分 display/touch public header 暴露 SDK/raw handle，后续需 source test/allowlist 管住。

- 2026-07-07：新增 `docs/context/knowledge/project/watch-interface-config-layering-review.md`，沉淀多 agent 对接口配置与层级衔接的专项审查。结论为主线分层仍成立，但需优先收敛 tracked `sdkconfig` 默认 watch device token、`watch_contract.v1.json` 未覆盖 `/v1/watch/ws` 与 `/v1/watch/sync`、`network_manager_get_status()` getter 副作用、BLE enable UI 重路径、`generated/widgets_init.c` 直接读 service、SoftAP 门户承接 Memory Watch 配置 schema、`imu_service` 直连 `qmi8658c`、天气模块混层、`network_service` 兼容面偏宽、`memory_watch_service` public API 膨胀、public header 泄漏 SDK/raw handle 以及 `check_layering.py` 覆盖不足。

- 2026-07-07：新增 `docs/context/knowledge/project/watch-framework-layering-review.md`，沉淀多 agent 对当前 ESP32-S3 手表固件框架分层的审查结论。结论为主干 `App/UI -> Service -> Manager/Domain -> Driver Adapter -> Vendor/SDK` 仍成立，不建议推倒重来或新增大 `ResourceManager`；但需优先收敛 `network_manager_get_status()` getter 副作用、BLE enable UI 重路径、`memory_watch_service` 膨胀、runtime gate 边界未文档化、`network_service` 兼容面偏宽、`lvgl_port` 反向通知 UI policy、天气模块混层和真实 sleep 前 blocker 事实源不足。

- 2026-07-07：按用户要求将 QMI8658C 路线收口为“统一配置 + 物理六轴读取”，彻底移除当前固件 WoM 运行路径。`qmi8658c` public API 保留 `init/init_bus/probe/config/read`，删除 `enable_wom/disable_wom/read_wom/read_int` 与 WoM/status public 类型；`qmi8658c_config_t` 新增 `int1_source/int2_source` 预留字段且第一版只接受 `QMI8658C_INT_SOURCE_DISABLED`；`qmi8658c_read()` 在未成功配置前返回 `ESP_ERR_INVALID_STATE`。`imu_service` 改为只做 `probe -> qmi8658c_config()`，默认最大量程 `accel_fs=3`（±16g）、`gyro_fs=7`（±2048 dps），不连续采样、不安装 GPIO21 ISR、不做 WoM fallback；`board_imu` 继续只保留 INT1/GPIO21 板级事实。同步更新 IMU active plan 与 QMI8658C knowledge，将旧 WoM/INT1 闭环标为历史证据。验证：source tests 22 passed；`git diff --check -- . ':!managed_components'` 通过，仅 LF/CRLF warning；`idf.py build` 通过，`111.bin` `0xac3230`，app free `0x33cdd0`/23%；`idf.py -p COM7 app-flash` 通过并完成 hash 校验；context standard 校验错误 0、警告 0。attempt log：`docs/context/runs/2026-07-07-attempt-qmi8658c-unified-config-remove-wom.md`。

- 2026-07-07：修复 context_librarian 审查发现的上下文管理问题。`query.py --scope archive` 现在显式视为历史检索，不再被 archived lifecycle 过滤；`check.py` 新增 active plan 门禁，拦截 `plans/active/` 中非 active 状态或 Progress 全勾选的计划。将已完成的 `2026-05-16-mini-games-porting-plan.md`、Hermes V2.1/V2.2/V2.4 三个执行计划从 `plans/active/` 迁入 `plans/completed/` 并标记 `status: archived`；同步修正 archived handoff 说明和相关 run 的计划引用。

- 2026-07-07：新增 `plans/active/2026-07-07-axp2101-rail-control-evidence-plan.md`，梳理 AXP2101 通过 DCDC/LDO 控制外设断电的安全推进路线：当前软件仍保持只读观测，不直接写 `REG80+` 输出轨；后续先补只读 rail status dump 与 `rail -> device -> owner -> can_off` 映射，再选择低风险外设做默认关闭的 opt-in 断电实验。

- 2026-07-07：新增 `plans/active/2026-07-07-light-allowed-runtime-save-plan.md`，将 `LIGHT_ALLOWED` 收敛为运行态深省电候选：30 秒 `STANDBY` 保持现有基础省电，5 分钟无交互后第一版只让 LVGL 主循环进一步降到 2000ms；`power_service` 与 `network_service` 暂不新增动作，外设断电只列候选，不进入 ESP Light Sleep。

- 2026-07-07：按用户要求将 `imu_service` 的 QMI8658C 运行 profile 全部切到最大量程：WoM 加速度 `wom_accel_fs_code=3`（±16g）、运动窗口加速度 `motion_accel_fs_code=3`（±16g）、运动窗口角速度 `motion_gyro_fs_code=7`（±2048 dps）；ODR、WoM 阈值、窗口长度和 driver/board/API 边界不变。同步 source test 锁定最大量程编码。验证：IMU/QMI source tests 23 passed；`idf.py build` 通过，`111.bin` `0xac3230`，app free `0x33cdd0`/23%；`idf.py -p COM7 app-flash` 通过并完成 hash 校验。attempt log：`docs/context/runs/2026-07-07-attempt-imu-service-max-range-profile.md`。

- 2026-07-07：完成当前 COM7 板 QMI8658C INT1/GPIO21 GPIO 中断闭环测试。完整刷写分区表后，启动日志显示 `int1_gpio_ready gpio=21`、`wom_configured ... int1_usable=1`；90 秒监控内捕获 3 次 `wom_event source=irq gpio=21 level=1 statusint=0x02 status1=0x04`，均进入 `physical_6axis` 16 帧窗口并输出 `raise_result`，无 panic/watchdog。结论：当前 COM7 板 IRQ 链路可用；2026-06-04 COM3 “GPIO21 浮空/开路”只作为当时样板证据，不再泛化到当前板。首次 `app-flash` 失败由旧 8MB factory 分区表与当前 app 大小不匹配导致，完整 `idf.py flash` 后恢复。临时 IMU service 测试开关已恢复为关闭，并已重新 build/app-flash 默认固件到 COM7，恢复日志未出现 `imu_service:`/`wom_event`。attempt log：`docs/context/runs/2026-07-07-attempt-qmi8658c-int1-gpio21-com7-irq.md`。

- 2026-07-07：修复 `garden.py` 报出的 3 个 `broken_owner_refs`：Safety Monitor skeleton review 的计划 owner 改到已归档的 `plans/completed/2026-05-25-safety-monitor-background-completion-plan.md`；QMI8658C accel m/s² attempt 的旧 fall_detection active plan owner 改到当前 `plans/active/2026-06-05-imu-runtime-framework-plan.md`；危险样本 SD 闭环计划的 recorder owner 改为真实路径 `main/features/danger_detection/danger_sample_recorder.c`。

- 2026-07-07：按用户判断将 `docs/context/handoffs/` 从 active 上下文层退场。旧 `current-task.md`、`current-repo-state.md`、`handoff-template.md` 已移入 `docs/context/archive/handoffs/` 并标记 `status: archived`；后续跨会话接手状态优先维护在对应 `plans/active/` 的 Progress/Validation/Next Step，失败路线和特殊证据写入 `runs/`，稳定事实写入 `knowledge/`。同步更新 `AGENTS.md`、`INDEX.agent.md`、`README.md`、`knowledge-map.md`、`context-garden-policy.md`、相关 active plan 和 `scripts/context` 的 check/garden/query active 层规则。

- 2026-07-07：按用户关于 driver/BSP/service 解耦的要求，收窄 `board_imu` 职责。`board_imu_config_t` 现在只保存 QMI8658C I2C 地址、INT1 GPIO 和 IMU 安装方向中的表盘法向轴；WoM 阈值、运动窗口、终点姿态阈值等第一版运行策略迁入 `imu_service.c` 本地 `imu_service_profile_t` / `k_imu_service_profile`。`qmi8658c driver` 继续只负责芯片协议和物理量输出，不消费板级 GPIO 或产品阈值。验证：source tests 23 passed；`git diff --check` 仅 LF/CRLF warning；`idf.py build` 通过，`111.bin` `0xac3230`，最小 app 分区剩余 `0x33cdd0`/23%；context standard 校验错误 0、警告 0。attempt log：`docs/context/runs/2026-07-07-attempt-board-imu-hw-profile-split.md`。

- 2026-07-07：按用户确认的“类型/注释表达单位，字段只保留 `x/y/z`”方案，重规划 QMI8658C public header。`qmi8658c_accel_mps2_t` / `qmi8658c_gyro_dps_t` 改为 `qmi8658c_accel_t` / `qmi8658c_gyro_t`，`qmi8658c_read_sample()` 改为唯一采样出口 `qmi8658c_read()`，删除公开单独 accel/gyro read API；`init/config/status/wom` 入口同步简化为 `qmi8658c_init_bus()`、`qmi8658c_config()`、`qmi8658c_read_wom()`、`qmi8658c_read_int()`、`qmi8658c_enable_wom()`、`qmi8658c_disable_wom()`。`imu_service` 改用 `sample.accel.x` / `sample.gyro.x`，WoM 触发帧读取完整六轴后只缓存 `last_wom_accel`，日志字段继续保持 `accel_mg` / `gyro_mdps`。验证：source tests 23 passed；`git diff --check` 仅 LF/CRLF warning；`idf.py build` 通过，`111.bin` `0xac3230`，最小 app 分区剩余 `0x33cdd0`/23%；context standard 校验错误 0、警告 0。attempt log：`docs/context/runs/2026-07-07-attempt-qmi8658c-concise-public-api.md`。

- 2026-07-07：按 framework review 将 QMI8658C 采样输出收口为物理量契约。`qmi8658c.h` 不再公开 `qmi8658c_raw_sample_t`、`qmi8658c_read_raw()` 或 raw-to-gyro conversion API；新增 `qmi8658c_sample_t` 与 `qmi8658c_read_sample()`，driver 内部仍用一次连续寄存器读取完成 `m/s^2` 与 `deg/s` 换算。用户复查指出 `dps/radps` 两套角速度 API 重复后，继续删除公开 `qmi8658c_gyro_radps_t` 与 `qmi8658c_read_gyro_radps()`，第一版对外只保留 `deg/s`。`board_imu` 删除 `accel_lsb_per_g`，将表盘法向阈值迁到 `face_axis_threshold_mg=-397`；`imu_service` snapshot 改为 `last_wom_accel_mps2` / `last_final_sample`，运动窗口、final pose 和日志改用 `physical_6axis`、`accel_mg`、`gyro_mdps`，不再依赖 raw LSB。同步更新 `scripts/extract_imu_raise_samples.py` 与 source tests。验证：`tests.test_qmi8658c_source tests.test_imu_service_source tests.test_imu_motion_source tests.test_extract_imu_raise_samples_script` 23 passed；`git diff --check` 仅 CRLF warning；`idf.py build` 通过，`111.bin` `0xac3230`，最小 app 分区剩余 `0x33cdd0`/23%。attempt log：`docs/context/runs/2026-07-07-attempt-qmi8658c-physical-sample-contract.md`。

- 2026-07-07：补齐 QMI8658C driver 级陀螺仪物理量换算。新增 `qmi8658c_gyro_dps_t` / `qmi8658c_gyro_radps_t`，以及 raw 六轴样本无 I2C 转换接口 `qmi8658c_convert_raw_gyro_dps()` / `qmi8658c_convert_raw_gyro_radps()` 和直接读取接口 `qmi8658c_read_gyro_dps()` / `qmi8658c_read_gyro_radps()`。driver 记录最近一次 `gyro_fs`，按 `±16..±2048 dps` 对应 `2048..16 LSB/dps` 换算，并用 `0.017453292519943295f` 输出 `rad/s`；仍不做板级轴向映射。验证：`tests.test_qmi8658c_source tests.test_imu_service_source tests.test_imu_motion_source` 22 passed；`git diff --check` 仅 CRLF warning；`idf.py build` 通过，`111.bin` `0xac3230`，最小 app 分区剩余 `0x33cdd0`/23%。attempt log：`docs/context/runs/2026-07-07-attempt-qmi8658c-gyro-physical-units.md`。

- 2026-07-06：清理旧摔倒检测模型接入路线。已从当前固件仓库删除失败的 Edge Impulse 208622 外部 `.espdl` 部署残留：`components/fall_detection_inference/`、`main/services/fall_detection_board_test.*`、对应 source test、旧 active plan 和旧 runner attempt 文档；同时撤掉 CMake/Kconfig/app_main/sdkconfig/tests 路由中的 `CONFIG_FALL_DETECTION_BOARD_TEST` 与旧 runner 引用。后续职责边界改为：`D:\esp32S3\imu` 负责训练、评估、导出，`D:\esp32S3\111` 只负责嵌入未来已验证可被 ESP-DL loader 加载的部署资产。保留 QMI8658C `m/s^2` driver API 和“模型输入坐标系”口径作为未来部署地基；禁止修改 `managed_components`。

- 2026-07-06：按用户决策将 QMI8658C driver 扩展为可直接输出三轴加速度 `m/s^2`。新增 `qmi8658c_accel_mps2_t` 与 `qmi8658c_read_accel_mps2()`，driver 内部根据最近一次 `accel_fs` 配置使用 `16384/8192/4096/2048 LSB/g` 和 `9.80665f` 完成 raw-to-physical 换算，并拒绝没有换算表的加速度量程编码；`qmi8658c_read_raw()` 暂保留给现有抬腕/排障链路。验证：`tests.test_qmi8658c_source tests.test_imu_service_source tests.test_imu_motion_source` 21 passed；`idf.py build` 通过，`111.bin` `0xac3230`，app free `0x33cdd0`/23%；本机仅探测到 `COM1`，未执行 `app-flash`。attempt log：`docs/context/runs/2026-07-06-attempt-qmi8658c-accel-mps2-api.md`。

- 2026-07-05：麦克风测试真机反馈显示录音文件可写出但输入幅度极低。两次测试均完整读取 `480000/480000` 字节并生成 `/sdcard/mic_tests/*_mic_raw.wav`，但 `ch0_rms≈40~42`、`ch0_peak≈182~194`，用户回放反馈“几乎听不到声音”。已增强 `audio_mic_test_service` 诊断：串口逐通道打印 `rms/peak/zero_pct/clip/samples/role`，记录 36dB 增益设置结果，并在 M 通道无输入但其他通道有输入时报告 `通道不匹配`。验证：`tests/test_audio_mic_test_service_source.py tests/test_audio_codec_port_source.py` 14 passed；完整 `idf.py build` 通过，`111.bin` `0xac3230`，最小 app 分区剩余 `0x33cdd0`/23%。attempt log：`docs/context/runs/2026-07-05-attempt-audio-mic-test-ui.md`。

- 2026-07-05：危险识别页新增手动麦克风测试入口。新增 `audio_mic_test_service`，UI 点击“测麦克风”后由一次性 FreeRTOS task 暂停 Safety Monitor、申请 `AUDIO_CODEC_OWNER_AUDIO_RECORDER`、以 36dB 增益录制 5 秒硬件原始 PCM（24kHz/2ch/16-bit/MR），串口输出 RMS/peak/zero/clip，SD 写入 `/sdcard/mic_tests/<timestamp>_mic_raw.wav` 与 JSON 报告；UI 只调用 service API 和读取 snapshot，不直接读麦克风或写 SD。验证：相关 source tests `32 passed`；host preview 截图 `main/ui/agent_preview/artifacts/danger-mic-test-preview.png` 无明显重叠；完整 `idf.py build` 通过，`111.bin` `0xac3070`，最小 app 分区剩余 `0x33cf90`/23%。attempt log：`docs/context/runs/2026-07-05-attempt-audio-mic-test-ui.md`。

- 2026-07-05：新增危险识别 `sensitivity_mode` 三档灵敏度。`danger_detection_service` 持有用户级 `保守 / 标准 / 敏感` 模式并映射 ESP-DL 单窗 danger 阈值 `0.95 / 0.90 / 0.85`，默认 `标准=0.90` 保持当前行为；`espdl_model_runner` 增加原子 threshold 与 `espdl_model_runner_set_threshold()`，`espdl_audio_runtime_set_danger_threshold()` 负责运行前保存和运行中下发；危险识别页新增三段式选择控件，UI 不显示原始阈值数字；host preview 新增 `-OpenDanger` 并截图到 `main/ui/agent_preview/artifacts/danger-sensitivity-preview.png`。验证：目标 source tests `24 passed`；完整 `idf.py build` 通过，`111.bin` `0xac1d80`，最小 app 分区剩余 `0x33e280`/23%。attempt log：`docs/context/runs/2026-07-05-attempt-danger-sensitivity-mode.md`。

- 2026-07-05：危险提醒接入首次强震 owner。新增 `features/alerts/haptic_alert_player.c/.h`，作为震动提醒 owner，使用短生命周期 FreeRTOS task 异步执行 `220ms on -> 90ms off -> 220ms on`，退出前兜底关闭 DS2413 马达；`app_alert_manager` 在新的 `APP_ALERT_SEVERITY_DANGER` 首次 raise 时触发 haptic，重复同源 active 告警不重复震动，震动失败不阻断红屏、提示音、手机通知或 SD 样本保存。验证：相关 source tests `36 passed`；`idf.py reconfigure` 后完整 `idf.py build` 通过，`111.bin` `0xac1380`，最小 app 分区剩余 `0x33ec80`/23%。attempt log：`docs/context/runs/2026-07-05-attempt-danger-alert-initial-haptic.md`。

- 2026-07-04：按用户要求将 `agent_serial_monitor` 回归为“完整日志采集工具”。工具不再输出 `status=fail`、`hard_fatal_count`、`diagnostic_event_count`、`fatal`、`diagnostic_events`、`custom_evidence`、`important_events` 或 tail 摘要；`-Preset/-Pattern/-LiteralPattern/-AllowNoBoot` 从 wrapper 与 Python CLI 移除。summary 仅保留采集事实、完整 `log_path` 和残留 monitor 清理结果；`status=captured` 只表示工具完成采集，不代表固件健康。新增明确 panic 结构提前结束：看到 panic 起点并确认 `Backtrace:` / `ELF file SHA256` / `Rebooting...` / register dump 后，记录 `panic_log_seen=1`、`capture_stop_reason=panic_log_seen` 并提前收尾。

- 2026-07-04：增强 agent 串口采集工具收尾诊断与清理。`scripts/board/agent_serial_monitor.py` 现在在采集结束后自动全局扫描残留 `idf_monitor.py`、`idf.py ... monitor`、`ESP_IDF_MONITOR_TEST` 相关 host 进程并结束匹配到的 PID；扫描不限定 `COM3`，会尽量从命令行提取任意 `COMx` 到 `residual_monitor_processes[].port` 并在终端残留行显示。summary 新增 `residual_monitor_count/residual_monitor_processes/residual_monitor_scan_error/residual_monitor_kill_results/residual_monitor_killed_count/residual_monitor_remaining_count/residual_monitor_remaining_processes`，终端输出新增 `AGENT_SERIAL_MONITOR_RESIDUAL_COUNT/KILLED_COUNT/REMAINING_COUNT`。同步更新 `AGENTS.md` 与 `agent-operational-rules.md`：除非 `agent_serial_monitor` 不可用或已明确失败并记录原因，否则不得直接调用裸 `idf.py monitor`，也不得用 `Start-Process` 后台启动 monitor；后续 agent 先看 summary 判断串口占用风险。

- 2026-07-04：`agent_serial_monitor` app-flash-monitor 真机闭环发现 `diagnostic_events.wifi_disconnect_reason` 的旧正则 `reason=[0-9]+` 会误伤 `power_budget_change: standby_reason=0`。已收窄为仅匹配真实断连语义（`STA 断开连接` / `disconnect` / `disconnected` 且含 `reason=`），并补 source test 覆盖电源日志不误报、Wi-Fi 断连仍计数。

- 2026-07-04：从 `59e29713` 抽取并合入 DS2413 马达板级能力，不 cherry-pick 测试入口或 scratch 迁移目录。新增 `components/ds2413` 最小 1-Wire 驱动、`main/app/board_ds2413_motor.c/.h` 板级封装，GPIO18 只使用 RMT backend（已按用户要求删除 UART1 兜底），FreeRTOS mutex 串行化访问，初始化成功后立即写 PIOA pull-low 默认关闭态；`hardware_init()` 在 NVS 后尽早调用，失败只 warning 不阻断启动。补充 DS2413 公开接口、RMT-only、mutex、open-drain 语义和协议字节注释；`main/idf_component.yml` 新增 `espressif/onewire_bus: ^1.1.1`。验证：`python -m unittest tests.test_board_ds2413_motor_source tests.test_nonblocking_boot_source tests.test_power_integration_source` 27 passed；`idf.py build` 通过，`111.bin` `0xac7aa0`，app free `0x338560`/23%。attempt log：`docs/context/runs/2026-07-04-attempt-ds2413-motor-board-init.md`。

- 2026-07-04：完成 DS2413 马达板级能力闭环板测，并修复一次配置漂移。闭环时发现 `sdkconfig` 仍开启 `CONFIG_RUNTIME_RESOURCE_GATE_BOARD_TEST=y`，正常固件会自动跑 runtime gate 板端压测；已恢复为默认关闭。按规则执行 `idf.py fullclean; idf.py build` 通过（`111.bin` `0xac7130`，app free `0x338ed0`/23%），COM7 `app-flash` 成功，pyserial 复位日志 `board_logs/2026-07-04-20-47-24-ds2413-normal-com7-pyserial.log` 证明 `DS2413 ROM via RMT: BA DC CD 73 50 05 10 46`、`DS2413 motor default off: raw=0x78 PIOA(state=0 latch=0)`、`startup_sequence_done`、`ui_first_frame_ready`、`cold_boot_resource_snapshot_done`，且无 `runtime_gate_test`、Guru、panic、watchdog、`ESP_ERR_NO_MEM`。

- 2026-07-04：基于 `docs/context/INDEX.agent.md` 的 Domain Routing 与当前项目框架，补充 5 个项目级 Codex 只读复查 agent 到 `.codex/agents/`：`ui-lvgl-reviewer`、`network-provisioning-reviewer`、`power-runtime-reviewer`、`danger-detection-reviewer`、`hermes-memory-watch-reviewer`。这些 agent 分别覆盖 UI/LVGL/触摸字体、网络配网、低功耗与资源预算、危险识别/Safety Monitor、AI Memory Watch/Hermes 边界；保留原有 `context_librarian`、`framework_reviewer`、`embedded_safety_reviewer`、`verification_runner` 作为横向 agent。

- 2026-07-04：COM3 板端自测已通过后，按用户要求删除危险样本 recorder 临时板端自测代码。移除 `danger_sample_recorder_board_test.c/.h`、`CONFIG_DANGER_SAMPLE_RECORDER_BOARD_TEST*`、`app_main` 自测入口调用、CMake 源文件接线和对应 source test；正式 recorder、ESP-DL PCM tap、Alerting capture 与 SD WAV/JSON 写入链路保留。板测证据仍见 `docs/context/runs/2026-07-04-attempt-danger-sample-recorder-board-test.md` 与 `board_logs/2026-07-04-03-21-14-danger-sample-recorder-board-test.log`。attempt log：`docs/context/runs/2026-07-04-attempt-danger-sample-recorder-board-test-cleanup.md`。

- 2026-07-04：新增并实测危险样本 recorder 板端自测入口。`CONFIG_DANGER_SAMPLE_RECORDER_BOARD_TEST` 默认关闭，开启后启动 60 秒注入合成 16kHz mono PCM，调用 `danger_sample_recorder_capture(1U, 0.95f, 32000ULL)`，等待 SD worker 写出 WAV/JSON，并通过 `/sdcard/danger_samples` 文件数量增量确认成功。验证：相关 source tests `31 passed`；正常固件 `idf.py build` 通过（`111.bin` `0xabff00`，app free `0x340100`/23%）；独立测试固件 `build-danger-sample-test` 构建通过（`111.bin` `0xac0810`）；COM3 app-flash 后日志 `board_logs/2026-07-04-03-21-14-danger-sample-recorder-board-test.log` 出现 `DANGER_SAMPLE_BOARD_TEST: DONE wav_before=3 wav_after=4 json_before=3 json_after=4`，生成 `/sdcard/danger_samples/20260704/032222_1_95.wav/.json`，未见 FAIL/Guru/panic；收尾已刷回默认关闭测试的正常固件。attempt log：`docs/context/runs/2026-07-04-attempt-danger-sample-recorder-board-test.md`。

- 2026-07-04：用户补充确认危险样本 SD recorder 已做板端实测，不是仅 host 仿真。当前口径更新为：`Alerting -> recorder capture -> /sdcard/danger_samples` 的真机样本保存链路已由用户实测通过；本轮对话未保存原始串口日志、WAV/JSON 文件名或 SD 卡截图，因此后续引用该证据时标注为用户反馈实测，而不是脚本 artifact。此前新增 host 仿真脚本仍作为无人值守回归工具保留。

- 2026-07-04：新增无人值守危险触发 host 侧仿真脚本 `scripts/danger_detection/simulate_danger_trigger.py`。脚本不访问硬件、网络或 SD 卡，通过纯 Python 行为模型模拟连续 danger 触发 Alerting、recorder 前 1 秒 + 后 1 秒 capture、post backfill、早期窗口跳过和 stop/reset 取消 pending，并输出包含 safe_mode、触发参数、响应详情和断言结果的 JSON 报告。新增 `tests/test_danger_trigger_simulation_script.py` 覆盖脚本可无人值守运行和报告字段。验证：脚本生成 `artifacts/danger_trigger_sim/report.json`，相关危险识别测试 `28 passed`。

- 2026-07-04：修复危险样本 recorder 阶段 1A-1D 的核心语义。ESP-DL PCM tap 改为在 `resample_24k_to_16k()` 后发布连续 `resampled_samples` chunk，推理结果新增 `window_end_sample_index`；`danger_detection_service` Alerting capture 传入窗口末尾 index，普通 stop 改为 reset recorder session 而不是 deinit；`danger_sample_recorder` 改为 3 秒 PSRAM ring，capture 复制前 1 秒并 pending 收集后 1 秒，满 32000 样本后通过 worker 写 WAV/JSON，继续使用 `.tmp -> sd_manager_rename_file()` 提交。子代理复查后补修两点：去掉 ESP-DL PCM tap 的不兼容函数指针强转，改用 service adapter；capture 时回填 ring 中已存在的 post 样本，避免依赖默认 chunk 对齐。验证：相关 source tests `26 passed`；`. D:\esp-idf\v5.5.3\esp-idf\export.ps1; idf.py build` 通过，`111.bin` `0xabfea0`，最小 app 分区剩余 `0x340160`/23%。attempt log：`docs/context/runs/2026-07-04-attempt-danger-sample-recorder-pending-capture-fix.md`。

- 2026-07-03：`POST /v1/watch/alerts` 已从免鉴权演示接口收口为设备 token 鉴权接口。server route 复用 `_require_device()`，按 body `device_id` 校验 `Authorization: Bearer <device_token>`，未授权不广播到 Android App；新增/扩展 `tests/test_app.py` 覆盖 alerts 缺 token、错 token、未知设备和合法 token 成功路径。验证：watch endpoint server tests `144 passed`；Docker 容器重建成功；公网无 token POST 返回 `401 missing_bearer_token`，合法 token 公网 smoke 返回 `HTTP 200 ok=True`，验证未打印真实 token。

- 2026-07-03：危险识别手机通知第一版完成真机端到端确认。用户反馈“真机测试没问题”，确认 `ESP32 Alerting -> watch_endpoint_service -> HTTPS POST /v1/watch/alerts -> watch_voice_endpoint -> Android App WSS -> 手机通知栏` 链路可用；未保存原始串口日志、设备 token 或手机截图。`docs/context/runs/2026-07-02-attempt-danger-alert-cloud-post.md` 状态更新为 `completed` / `observed`。后续正式化重点：`/v1/watch/alerts` 设备 token 鉴权、Android App 断线重连与前台服务状态提示。

- 2026-07-02：修复表情预览帧进入 LittleFS `resources` 分区导致 `idf.py build` 在 `resources.bin` 生成阶段报 `LFS_ERR_NOSPC`。删除 tracked `resources/watchface`，将 `extract_watchface_frames.py` 默认输出改为 `sdcard/watchface/frames`，将 `pack_watchface_rawanim.py` 默认输入改为 `sdcard/watchface/frames`，并用 `.gitignore` 忽略预览帧。验证：`resources/` 当前约 `3.07 MiB`，`tests/test_watchface_resource_boundary_source.py` `2 passed`，`idf.py build` 通过，LittleFS 创建日志只包含 `fonts` 和 `weather`。attempt log：`docs/context/runs/2026-07-02-attempt-watchface-resources-nospc.md`。

- 2026-07-02：ESP32 危险识别 `Alerting` 接入云端手机通知链路，并将危险告警 worker 从 `memory_watch_service` 迁到中性 `watch_endpoint_service`。`danger_detection_service` 在 Edge raise / ESP-DL 连续窗口确认首次进入告警时投递 `watch_endpoint_service_post_danger_alert()`；`watch_endpoint_service` 现在自己持有单槽静态 FreeRTOS queue 和 `watch_alert` PSRAM worker，复用 `memory_watch_service_copy_endpoint_config()` 读取 endpoint 配置快照后异步调用 `memory_watch_voice_client_post_danger_alert()`；`memory_watch_service` 不再暴露 `memory_watch_service_post_danger_alert()`，只继续作为 endpoint 配置/NVS owner。验证：相关 source tests `44 passed`；`. D:\esp-idf\v5.5.3\esp-idf\export.ps1; ninja -C build __idf_main` 通过；完整 `idf.py build` 通过，`111.bin` `0xabde50`，最小 app 分区剩余 `0x3421b0`/23%。attempt log：`docs/context/runs/2026-07-02-attempt-danger-alert-cloud-post.md`。

- 2026-07-01：补齐 SD manager 文件 API。`components/sd_card/sd_manager.c` 现在实现头文件已有声明的 `sd_manager_read_file/write_file/create_dir/delete_file/get_file_size`，基于 VFS `fopen/fread/fwrite/mkdir/remove/stat` 操作 `/sdcard`；新增 `tests/test_sd_manager_source.py` 锁定声明与实现一致。验证：新增 source test `2 passed`；`idf.py build` 未进入编译阶段，CMake component manager 拉取 `esp_codec_dev.json` 返回空/非法 JSON，与本次代码改动无关。attempt log：`docs/context/runs/2026-07-01-attempt-sd-manager-file-api.md`。

- 2026-07-01：完成表情表盘 raw animation 打包器 `scripts/watchface/pack_watchface_rawanim.py`。脚本从 `resources/watchface/frames` 打包 RGB565 little-endian raw animation，带 `8M` resources 阈值；实测 5 个状态 raw 总量 `22,238,518 bytes`（含 manifest），超过用户指定“超过 8M 就直接 SD 卡”的阈值，因此输出到 `sdcard/watchface/` staging，而不是写入 `resources/watchface`。

- 2026-07-01：表情表盘资源路线再次调整：先做 `resources` / LittleFS 板端测试，过了再考虑 SD 卡；允许按测量结果扩大 `resources` 分区，但必须记录新分区布局并全量刷新相关分区。SD 卡保留为体积、刷写或读取性能不理想时的第二阶段备选。

- 2026-07-01：表情表盘板端资源路线改为 SD 卡优先。`/sdcard/watchface` 作为最终动画资源仓库，`resources/watchface/frames` 仅保留为电脑端预览/生成中间产物；运行时仍必须先把当前动画帧加载到 PSRAM，再通过内存 `lv_image_dsc_t` 给 LVGL 绘制，不开启 LVGL FATFS 直读路径，不为表盘扩大 `resources` 分区。

- 2026-07-01：完成表情表盘电脑端离线拆帧预览。新增 `scripts/watchface/extract_watchface_frames.py`，从 `D:\esp32S3\表情包` 的 V1 五个 GIF 生成 `resources/watchface/frames/` 下的 `410x502` 白底 PNG 检查帧、`manifest.json` 和 `contact_sheet.png`；当前输出是视觉检查中间产物，不是最终板端动画包。

- 2026-07-01：表情表盘计划从“V1 单帧状态图”升级为“V1 低帧率短循环动画”。active plan 明确采用 SD 卡 `rawanim` 或等价动画包、PSRAM 多帧槽、后台加载/解码、LVGL timer 只切 ready 帧；禁止因资源压力退回半尺寸拉伸或静态单帧。

- 2026-07-01：重写 active plan `docs/context/plans/active/2026-06-30-watchface-emoji-root-ui-plan.md`。用户已回退上一轮表情表盘代码，当前计划改为“从干净架构重执行”：正式 UI 不继续修补旧 `watchface_view`，禁止 LVGL draw 阶段直接从文件系统流式读取全屏大图；后续必须先把活动帧加载到 PSRAM，并通过内存 `lv_image_dsc_t` 渲染，以规避上一轮 `Cache disabled but cached memory region accessed` 崩溃。

- 2026-07-04：加固 `agent_serial_monitor` 防误用语义：`app-flash-monitor` 默认静默控制台，新增 `--stream-console` / `-StreamConsole` 显式打开长日志输出；`observed_no_boot` / `boot_missing_after_flash` 默认返回失败，只有 `--allow-no-boot` / `-AllowNoBoot` 才允许成功；summary 顶层增加 `flash_completed/boot_seen/app_started/startup_done/observation_complete/hard_fatal_count/diagnostic_event_count`，并将 `ESP_ERR_NO_MEM` 等资源事件拆到 `diagnostic_events`，避免和 `Guru/panic/watchdog` 硬失败混淆；新增 `--preset` / `-Preset` 支持 `standby/system-time/runtime-gate/wifi` 常用证据组合。
- 2026-07-29：完成 Watch Foreground Session Lifecycle 阶段 4-6 代码闭环：foreground gate 收敛为 fail-fast `try_acquire(owner)`；Hermes 与 official_chat 在 owner task 内最多等待 5 秒并支持离页取消；`background_service_manager` 新增 quiesce generation/ACK，使用静态 EventGroup 等待 Safety Monitor/ESP-DL 让路确认；BLE/SoftAP provisioning 继续由 network owner task 异步管理；组合 board test 增加 gate/quiesce/heap/PSRAM 观测。聚焦 source tests `58 passed`，layering warning 0、known exception 2，ESP-IDF build 通过，`111.bin=0xabe5c0`、app free `0x341a40`（约 23%）。COM7 最终冷启动回归待本次文档闭环后执行。执行记录：`docs/context/runs/2026-07-29-attempt-foreground-switch-quiesce-regression.md`。

- 2026-07-29：复查并收紧 Watch Foreground Session Lifecycle 阶段 6：`finish_foreground_quiesce(generation)` 只允许当前代次结束请求，等待 ACK 同时要求 generation、`danger_quiesced`、后台 runtime 已停止和 `last_error == ESP_OK`；BLE provisioning 自动完成或启动失败后按用户偏好恢复普通 BLE presence。聚焦 source tests `57 passed`，layering warning 0、known exception 2；ESP-IDF build 通过，`111.bin=0xabe680`、app free `0x341980`（约 23%）。COM7 最终冷启动回归待文档校验后执行。执行记录：`docs/context/runs/2026-07-29-attempt-foreground-switch-quiesce-regression.md`。

- 2026-07-29：完成 Watch Foreground Session Lifecycle 本轮冷启动回归。COM7 `app-flash-monitor` 45 秒刷写采集成功，`panic_log_seen=0`、`residual_count=0`，日志到达 `network_service_ready`；board test 观测 `internal_free=76971`、largest=55296`、psram_free=6803516`，未发现 panic/Guru/WDT/NO_MEM/assert/stack overflow。全量 source tests `438 passed`，ESP-IDF build `111.bin=0xabe680`、app free 0x341980`（23%）。真实 Wi-Fi 页面 BLE/SoftAP provisioning 交互仍未由脚本代替验收。日志：`board_logs/2026-07-29-19-22-03-foreground-session-lifecycle-final.log`。

- 2026-07-29：复跑 Watch Foreground Session Lifecycle 文档闭环与 COM7 冷启动回归。context standard 校验错误 0、警告 0；聚焦 foreground/Hermes/official_chat/BLE/board source tests `53 passed`；`git diff --check` 无 whitespace error，仅 CRLF/LF 提示；ESP-IDF build 通过，`111.bin=0xabe680`、app free `0x341980`（23%）。COM7 `app-flash-monitor` 45 秒复跑成功，`panic_log_seen=0`、`residual_count=0`，启动到 `startup_sequence_done`、`ui_first_frame_ready` 和 `SERVICE_READY`；冷启动快照 `internal_free=61423`、`largest=53248`、`psram_free=6777484`。真实 Wi-Fi 页面 BLE/SoftAP provisioning 交互仍需单独人工覆盖。日志：`board_logs/2026-07-29-19-46-16-foreground-session-lifecycle-final-rerun.log`。
- 2026-08-02：音乐服务已通过香港 1Panel 现有 Compose 部署到当前服务器 `103.42.182.35`；`ai-memory-watch-music-service` 使用回环 `127.0.0.1:18788`、非 root UID `10003`、独立数据目录 `/opt/ai-memory-watch/music-data`，OpenResty 仅开放鉴权 `/v1/music/*`。公网无凭据请求返回 `401`，`/health` 仍为 `404`。旧香港服务器 `156.233.234.206` 记录作废；真实网易云扫码与音频播放仍待固定流门禁后验证。
- 2026-08-02：用同一生产镜像启动临时隔离测试容器完成固定流服务器 smoke：来源读取、会话创建和带 `device_id` 的 `audio/mpeg` 流读取通过，收到 `96429` 字节；测试容器、数据库和音频文件已清理，生产 `MUSIC_TEST_MODE` 未改变。ESP32 真机播放仍待测试源或真实网易云账号链路。
- 2026-08-02：修复 music-service 暂停时 FFmpeg stdout 未完全收敛导致 HTTP 音频流长时间占用的问题；现在暂停会立即终止当前流，继续会生成新 stream，已通过本地 `10/10` Node 测试和香港临时 API 闭环验证。
- 2026-08-02：完成音乐固定流真机闭环取证：COM7 专用测试镜像从香港 music-service 获取 MP3，ESP32 缓冲 `32768` 字节后取得 `music_player` 音频输出 owner 并通过播放测试，随后正常释放；测试镜像未发现 panic、Guru、栈溢出或 `ESP_ERR_NO_MEM`。已恢复生产 music-service 配置、删除测试音频，并刷回默认不开机自动播放的普通固件。
