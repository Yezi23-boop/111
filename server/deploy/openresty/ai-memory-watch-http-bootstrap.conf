# ACME 验证阶段的临时 HTTP 配置；证书签发后由最终 HTTPS 配置替换。
server {
    listen 80;
    server_name hermes.934000.xyz watch.934000.xyz;

    location ^~ /.well-known/acme-challenge/ {
        root /usr/share/nginx/html;
        try_files $uri =404;
    }

    location / {
        return 404;
    }
}
